# 변경 이력

버전 규칙: 구조 변경·큰 기능은 정수(v8 → v9), 작은 개선은 소수점(v8.0 → v8.1).
`apps-script/GuildManager_v*.gs` 의 `VERSION` 상수와 항상 일치해야 합니다.

---

## v11.6.6 — 파란색을 걷어냈습니다

> 시트는 그대로입니다. `.gs` 를 다시 붙여넣을 필요가 없습니다.

### 왜 파란색이 겉돌았나

이 앱에서 **색은 전부 뜻을 가지고 있습니다.**

| 색 | 뜻 |
|---|---|
| 🟠 주황 | 아직 못 받은 돈 |
| 🟢 초록 | 받은 돈 |
| 🔴 빨강 | 되돌릴 수 없음 |
| 🟡 금 | 돈의 단위 · 장식 |

그런데 청록만 아무 뜻 없이 「누를 수 있다」를 말했습니다. **뜻 없는 색 하나가**
**뜻 있는 색들 사이에 끼어 있으니** 화면에서 겉돌았습니다.

그래서 누르는 것은 **색이 아니라 재질**로 바꿨습니다 — 짙은 청동/돌빛.
이제 화면에 남은 색은 전부 뜻이 있습니다.

- **밝은 금색이 아니라 짙은 청동**인 것이 중요합니다. 밝은 금은 주황과 섞여
  「받을 돈」이 묻힙니다 — 명도를 크게 벌렸습니다 (검사가 밝기 차이를 지킵니다).
- **분배 버튼은 그대로 주황**입니다. 돈이 움직이는 행동은 계속 눈에 띄어야 합니다.
- 헤더 돌바닥·상태표시줄에서도 푸른 기를 뺐습니다.
- 어두운 화면에서는 짙은 청동이 묻히므로 **밝은 청동으로 뒤집습니다.**

### 남아 있던 이모지

잔액·연합 화면의 요약 숫자 라벨, 「정산 완료」 절 제목, 문장 가운데의  ·
 까지 글리프로 바꿨습니다. 문자열로 이어 붙이던 자리라 JSX 조각으로
풀어야 했습니다.

---
## v11.6.5 — 인터페이스 일체화: 화면 전체를 글리프 한 벌로

> 시트는 그대로입니다. `.gs` 를 다시 붙여넣을 필요가 없습니다.

홈 아이콘만 글리프였던 것을 **화면 전체**로 넓혔습니다.
절 제목 · 버튼 · 입력칸 라벨 · 팝업 제목 · 헤더 칩 — 전부 같은 규격(24 격자, 선 굵기 1.6)의
금선 글리프 **49종**입니다.

| | |
|---|---|
| 사전은 **손대지 않았습니다** | 세 언어가 한 줄에 묶여 있어 한쪽만 고치면 그 언어만 이모지가 남습니다 |
| 대신 **그리는 자리에서** 바꿉니다 | `components/IconText.tsx` 가 앞머리 이모지만 글리프로 |
| 시트가 보내는 문구도 함께 | 도구 이름 등에 붙어 오는 이모지도 같은 길로 통일됩니다 |

**일부러 남긴 것**

- **✅ · ⚠️ · ❌ 는 그대로입니다.** 장식이 아니라 신호이고, 시트의 `_adapterResult` 가
  앞머리 ✅ 로 성공·실패를 판정합니다 (규칙 5-3). 글리프로 바꾸면 관리 도구가 전부 실패로 보입니다.
- **공유용 글(카톡으로 나가는 텍스트)의 이모지**도 그대로입니다. 그림이 사라지면
  받는 사람이 무슨 목록인지 알기 어려워집니다.
- **표에 없는 이모지는 건드리지 않습니다.** 비슷해 보인다고 아무 글리프에나 이으면
  뜻이 바뀐 채 화면에 남습니다 (규칙 7).

### 🐛 그림만 있는 버튼이 낭독기에서 이름을 잃었습니다

🏷️ 버튼은 글자 없이 그림만 있습니다. 이모지 시절에는 그 글자가 이름을 대신했지만,
글리프는 `aria-hidden` 이라 **이름이 통째로 사라졌습니다.** `aria-label` 을 붙였습니다.
(촬영 스크립트가 그 버튼을 못 찾은 것이 이 문제의 증상이었습니다.)

---
## v11.6.4 — 전면 개편: 글리프 · 질감 · 창틀 · 글꼴

> 시트는 그대로입니다. `.gs` 를 다시 붙여넣을 필요가 없습니다.

### 🖼️ 아이콘을 직접 그렸습니다

이모지는 **기기마다 다른 그림**입니다. 안드로이드의 💰 는 초록 지폐 뭉치,
iOS 는 노란 주머니입니다 — 같은 앱을 쓰는 사람끼리 「돈 그림 눌러」가 통하지 않고,
색이 제각각이라 금색 화면에서 혼자 튑니다.

24 격자에 **선으로만** 그린 금색 글리프 8종으로 바꿨습니다. 어느 폰에서나 같은
그림이고 색도 우리가 정합니다. 홈 격자와 「지금 처리할 일」이 같은 글리프를 씁니다.

### 🔤 글꼴 — 명조에서 굵은 고딕으로

v11.6.3 에서 제목을 명조로 했는데 **옛날 문서처럼 읽혔습니다.** 게임의 인게임 UI 는
명조가 아니라 굵은 고딕입니다. 라이선스가 열린 고딕을 **우리 서버에 같이 실어** 냅니다.

> **리니지W 가 실제로 쓰는 글꼴은 실을 수 없습니다** — NC소프트 소유물입니다.
> 로고·아트워크도 쓰지 않았습니다. 시각 언어만 다시 만듭니다.

### 🪨 바탕 질감 · 창틀

- 바탕에 아주 옅은 결(SVG 잡음 한 조각). **그림 파일을 받아오지 않습니다.**
  글자가 놓이는 카드 위는 덮지 않습니다 — 대비가 떨어지면 숫자를 잘못 읽습니다.
- 화면 머리줄이 **게임 창의 제목 띠**가 됐습니다. 양옆 금 꺾쇠 + 아래 금 두 줄.

### 🐛 첫 화면에서 앱 이름이 바뀌는 것이 보였습니다

이름은 시트에 있어서 첫 그림은 이름이 오기 전에 그려집니다. 기본 이름을 띄웠다가
1초쯤 뒤 진짜 이름으로 갈아치웠고, **그게 눈에 보였습니다** — 제목이 저 혼자 바뀌는
화면은 잘못 들어온 것처럼 읽힙니다.

마지막에 본 이름을 기억해 첫 그림부터 그것을 씁니다. 시트에서 온 값이 언제나
이깁니다(마스터가 바꾼 이름이 안 따라오면 그게 더 나쁩니다). 브라우저 탭 제목도 같이 맞춥니다.
처음 여는 기기에는 기억해 둔 것이 없어 예전처럼 기본 이름이 잠깐 보입니다 —
없는 값을 지어내지는 않습니다.

---
## v11.6.3 — 리니지W 쪽으로 당긴 화면

> 시트는 그대로입니다. `.gs` 를 다시 붙여넣을 필요가 없습니다.

밝은 양피지 바탕은 그대로 두고(낮에 밖에서 숫자를 읽어야 합니다),
**모서리·테두리·글꼴**을 게임 쪽으로 당겼습니다.

| | 전 | 후 |
|---|---|---|
| 모서리 | 16px 둥근 | **6px 각진** (버튼·칩은 4px) |
| 카드 | 그림자만 | 테두리 + **위쪽 금 한 줄** |
| 절 제목 | 글자만 | 앞에 **금 표식** |
| 홈 아이콘 | 평평 | 위 모서리에 **금 눈금** |
| 버튼 | 단색 | **금테 + 옅은 명암** |
| 고른 칩 | 청록 | 청록 + **금테** |
| 팝업 | 둥근 머리 | **금 두 줄 + 금 손잡이** |
| 제목·금액 | 고딕 | **명조 계열** |

**일부러 안 바꾼 것**

- **버튼 색은 그대로 청록**입니다. 금색 버튼으로 바꾸면 「분배 전」(주황)과 섞여
  받을 돈이 묻힙니다. 금은 테두리·구분선·금액에만 씁니다.
- **폰트 파일을 싣지 않았습니다.** 한국어 세리프는 5MB가 넘어 첫 화면이 그만큼
  늦어지고, 게임 폰트를 복사해 오는 것도 아닙니다. 기기에 있는 명조만 씁니다 —
  없으면 본문 글꼴로 떨어지고, 굵기·자간·금색이 남아 그대로도 제목으로 읽힙니다.
- **리니지W 로고·아트워크는 쓰지 않았습니다.** 시각 언어만 CSS로 다시 만들었습니다.
- 참여자 칩의 안쪽 여백은 손대지 않았습니다 — 이름 폭 예산이 그 값을 전제로
  잡혀 있어 늘리면 이름이 잘립니다.

### 🎨 클로드 디자인에 디자인 시스템

`npm run design` 이 `app/globals.css` 를 **그대로 복사**해 묶음을 만듭니다
(색 · 글자 · 버튼 · 칩 · 카드 · 목록 줄 · 아이템 이름 · 팝업 8장).
부품을 디자인용으로 따로 그리지 않는 것이 요점입니다 — 두 벌로 두면 어느 쪽이
진짜인지 아무도 모르게 되고, 그 순간 디자인 시스템은 쓸모가 없어집니다.

---
## v11.6.2 — 아이템을 고르면 보스가 따라옵니다

> 시트는 그대로입니다. `.gs` 를 다시 붙여넣을 필요가 없습니다.

공식 게임정보의 드롭표(`manual/boss-drops.tsv`, 보스 153종 · 1,079건)를
**아이템 → 보스**로 뒤집어 앱에 실었습니다 (`lib/drops.ts`, 아이템 170종 · 짝 745개).

| 상황 | 앱이 하는 일 |
|---|---|
| 그 아이템을 주는 보스가 **하나뿐** (37종) | **자동으로 넣고**, 넣었다고 알려줍니다 |
| 보스가 **여럿** | 정하지 않습니다. 칩으로 늘어놓고 **사람이 고릅니다** |
| 표에 **없는** 아이템 | 아무 말도 하지 않습니다 |

- **관리자가 넣어둔 값은 절대 덮어쓰지 않습니다.** 고칠 때마다 되돌아오면 칸을 못 씁니다.
- 여기 없는 보스도 **그대로 칠 수 있습니다.** 제안일 뿐 입력을 막지 않습니다.
- 아이템 등록 · 연합 등록 · 🏷️ 고치기 세 곳 모두에서 됩니다.

**시트에 넣지 않은 이유**: 이건 길드 자료가 아니라 게임 자료입니다. 시트에 두면
길드마다 같은 표를 따로 채워야 하고, 게임이 바뀔 때마다 전부 고쳐야 합니다.
`npm run drops` 로 다시 만듭니다.

> 사전에 없는 보스 6종(드루가의 보물 상자 · 바포메트의 분신 · 베레스의 분신 ·
> 전설의 도전자 · 죽음의 사제 등)이 주는 아이템은 제안이 붙지 않습니다.
> 용어 사전에 그 보스를 넣고 `npm run drops` 를 다시 돌리면 살아납니다.

---

## v11.6.1 — 클래스로 좁혀 보기 · 공식 아이템 그림

> 시트는 그대로입니다. **`.gs` 를 다시 붙여넣을 필요가 없습니다** (세 번째 자리는 앱에만 있습니다).

### 🎯 클래스로 좁혀 보기 — 드롭다운 하나

서버는 칩(여러 개), 클래스는 **드롭다운(하나)** 입니다. 클래스는 13종이라
칩으로 깔면 서버 칩 아래에 13개가 더 붙어 정작 봐야 할 명단이 화면 밖으로 밀립니다.
"기사와 요정을 한 화면에서" 볼 일도 없습니다.

| 화면 | |
|---|---|
| 💰 잔액 | 서버 칩과 **AND** — 01서버의 기사 |
| 📦 아이템 | 참여자 고를 때. **이미 체크한 사람은 여기서도 안 숨습니다** |
| ⚙️ 관리 명단 | 위와 같음 |

- 기본값은 **전체**입니다 — 처음 여는 사람이 놀라지 않게.
- **"전체"와 "클래스 미지정"은 다른 값입니다.** 미지정을 고르면 클래스를 아직 안 넣은 사람만 나옵니다.
- 인원이 0인 클래스는 목록에 아예 안 나옵니다.

### 🖼️ 아이템에 공식 그림이 붙습니다

리니지W 공식 게임정보가 아이템마다 주는 그림 주소를 **아이템 337건 전부**에 넣었습니다.

- 짝짓기는 **국문 이름 정확 일치**로만 합니다. 안 맞으면 비웁니다 — 비슷한 이름에
  갖다 붙이면 엉뚱한 그림이 붙고 아무도 못 알아챕니다.
- **보스는 비어 있습니다.** 공식에 NPC 그림을 주는 길이 없습니다. 없는 것을 지어내지 않습니다.
- `npm run icons:official` 로 언제든 다시 뽑습니다.

### 🐛 클래스를 넣으면 한자가 국문보다 작아졌습니다

v11.5 에서 참여자 칩 둘째 줄을 `한자 · 클래스` **한 문자열로 합쳐** 크기를 정했습니다.
글자가 길어진 만큼 자동 축소가 걸려, 클래스를 넣은 순간 한자가 12px 로 쪼그라들어
국문(14px)보다 작아졌습니다. 중국 혈맹원에게는 **한자가 본명**이라 v10.8 부터
"한자가 더 커야 한다"가 규칙이었는데 조용히 깨져 있었습니다.

칩 폭은 64px 이라 큰 한자와 클래스를 한 줄에 담을 방법이 없습니다 — **줄을 나눴습니다.**
국문 / 한자 / 클래스 세 줄이 되고, 크기도 각자 정합니다.

### 🐛 이름에 공백이 끼면 아이콘만 안 나왔습니다

등급 테두리·티어는 이름을 정규화해서 찾는데(규칙 4) **그림만 정확 일치**로 찾고
있었습니다. 테두리는 뜨는데 아이콘만 안 뜨는, 원인을 짚기 어려운 상태였습니다.
셋 다 같은 길(`imgOf`)로 찾습니다.

---

## v11.6 — 레이드일 · 보스 · 루팅서버 · 루팅캐릭터

> ⚠️ **`.gs` 를 붙여넣고 [배포 관리] → 새 버전으로 재배포해야 합니다.**

### 🏷️ 아이템명에 몰아 적던 것을 칸으로 나눕니다

지금까지 이렇게 적어 왔습니다.

```
8/14 수룡 / 불변의 목걸이 / 차무식루팅
8/13 안타라스 / 전제비 / 11서버루팅
8/10 검은망령투구  / 3티어 / 6서버루팅
```

한 칸에 몰면 **패턴이 일정하지 않습니다** — 1·2번은 보스 자리인데 3번은 티어입니다.
그러면 "안타라스가 준 것만" 세거나 "차무식이 먹은 것만" 찾을 수가 없고,
아이템명이 사전과 안 맞아 등급 테두리·티어도 붙지 않습니다.

| 칸 | 입력 | 사전 활용 |
|---|---|---|
| 레이드 날짜 | 날짜 선택 | — |
| 보스 | 자동완성 | 사전 **보스만** 제안 (아이템에 묻히지 않게) |
| 아이템명 | 자동완성 | 사전 아이템 → **등급·티어 자동** |
| 루팅 서버 | 서버 칩 1개 | — |
| 루팅 캐릭터 | 명단 제안 + 자유 입력 | 명단에 없는 이름도 가능 |

- 티어 입력칸은 **없습니다.** 아이템명을 고르면 사전에서 따라옵니다.
- 전부 **선택**입니다. 비우면 빈칸으로 남습니다.
- **옛 기록을 자동으로 쪼개지 않습니다.** '수룡'이 보스인지 아이템인지는 규칙으로
  정할 수 없습니다 — 원문을 보여주고 사람이 옮겨 적습니다 (규칙 7).

### 👥 관리자도 고칠 수 있습니다

`api_setAllianceMeta` · `api_setItemMeta` 를 **돈을 다루는 편집 함수와 따로** 만들었습니다.
이 둘은 새 4칸 말고는 손댈 길이 없어서, **정산·분배가 끝난 건이라도 관리자가** 고칩니다.

> 권한을 "누구인가"가 아니라 **"무엇을 만질 수 있는가"** 로 가릅니다.
> 화면을 고쳐 직접 불러도 이 경로로는 장부가 바뀌지 않습니다.
> `verify:gs` 가 이 함수들이 금액·적립액·상태 열에 접근하지 않는지 검사합니다.

### 🗂️ 시트 열 (모두 맨 뒤)

| 시트 | 추가 |
|---|---|
| `[연합]` | 레이드일 · 보스 · 루팅서버 · 루팅캐릭터 |
| `[아이템]` | 동일 |

- `서버`(B열)와 `루팅서버`는 **다른 것**입니다. 서버는 정산에 참여한 서버(여러 줄),
  루팅서버는 실제로 떨어진 서버(한 건에 하나)입니다.
- 옛 시트는 열이 모자라 `_widenTo` 로 먼저 넓힙니다. **안 넓히고 읽으면 그 화면이
  통째로 안 열립니다** — 특히 연합 목록 조회는 탭을 열 때마다 지나는 길입니다.

검사: `verify:gs` 80건(신규 1건 — 새 API 가 금액·적립액·상태를 못 만지는지 + 관리자 경계).

---

## v11.5 — 아이템 티어 · 혈맹원 클래스 · 서버 필터 통일

> ⚠️ **`.gs` 를 붙여넣고 [배포 관리] → 새 버전으로 재배포해야 합니다.**

### 🏷️ [용어] 시트에 '티어' 열 (맨 뒤)

빈칸과 `0티어`는 **다른 뜻**입니다.

| 값 | 뜻 | 예 |
|---|---|---|
| 빈칸 | 티어라는 개념이 없다 | 마법서 · 정수 · 비법서 |
| `0티어` | 장비인데 티어 표기가 없다 | 드래곤 슬레이어 · 지룡의 비늘 갑옷 |

뭉개면 마법서에 없는 등급이 생기거나, 진짜 0티어 장비가 묻힙니다.
공식 자료 기준으로 전설·신화 중 티어 빈칸 162건 가운데 **132건이 마법서·정수·비법서**,
**30건이 장비**였습니다.

- 티어 열은 **맨 뒤**입니다. 중간에 끼우면 이미 배포된 v11.4 시트의 이미지·비고가
  한 칸씩 밀려 읽힙니다. `_getOrCreateTerms` 가 옛 6열 시트에 머리글만 더해 줍니다.
- `api_bulkTerms` 가 티어를 함께 받습니다 — 붙여넣기 한 번으로 티어까지 들어갑니다.
- `_normTier` 가 `0`·`3티어`·`T2` 를 모두 `N티어` 로 맞추고, **알아볼 수 없으면 빈칸**입니다.
  틀린 티어가 박히는 것보다 비어 있는 편이 낫습니다 (규칙 7).

### 👤 [멤버DB] H열 '클래스' — 공식 13종

기사 · 요정 · 마법사 · 다크엘프 · 전사 · 군주 · 수라 · 총사 · 마격사 · 성기사 · 나찰 · 귀검사 · 혈법사

- 한 사람에 **하나**입니다.
- 목록 밖의 값은 거부합니다. `마법사`와 `법사`가 갈리면 클래스로 걸러 볼 때
  그 사람만 조용히 빠집니다.
- 목록이 `.gs`·`lib/client.ts`·모의 시트 **세 곳**에 있어 `verify:gs` 가 대조합니다.
- `_getMemberRows` 는 시트에 있는 만큼만 읽습니다 — 8열을 요구하면 G열까지인
  옛 시트에서 명단 읽기가 통째로 죽습니다.

### 🗂️ 서버 필터를 [연합]·[관리]에도

잔액·아이템에만 있던 칩을 네 화면이 같은 한 벌로 씁니다.

- 연합은 한 건이 여러 서버에 걸치므로 **하나라도 포함**되면 남깁니다
  (교집합이면 3서버 건은 세 칩을 다 골라야 보입니다).
- 칩의 숫자는 **거르기 전** 자료로 셉니다 — 걸러진 것으로 세면 고르는 순간 1이 됩니다.
- 관리에서 혈맹운영비는 어느 서버를 골라도 남습니다 (계정이라 서버가 없습니다).
- **레이드는 넣지 않았습니다** — `[레이드]` 시트가 요일·시간·보스뿐이라
  걸러낼 서버 데이터가 없습니다. 넣으면 눌러도 아무 일이 없는 버튼이 됩니다.

### 🎨 아이템 이름 — 등급 테두리 · 티어 배지 · 아이콘

- 전설 = 보라, 신화 = 황금, **사전에 없으면 검정**.
- 등급은 **사전의 분류로만** 정합니다. 이름을 보고 짐작하지 않습니다 —
  `전설 제작 비법서`(전설)와 `전설의 도전자`(보스)는 둘 다 '전설'로 시작합니다.
- 티어가 빈칸이면 배지를 아예 그리지 않습니다.
- 그림은 관리자가 [용어] 시트 `이미지` 칸에 넣은 주소만 씁니다 (우리가 긁어오지 않습니다).

검사: `verify:gs` 78건(신규 2건 — 클래스 목록 3곳 대조, 티어 정규화 9케이스 + 열 위치).

---

## v11.4 — 리니지W 용어 사전 (국문 · 中文 · English)

> ⚠️ **`.gs` 를 붙여넣고 [배포 관리] → 새 버전으로 재배포해야 합니다.**

### 📚 새 [용어] 시트 — 아이템·보스·서버 이름을 세 언어로

중국 혈맹원과 아이템 이야기를 할 때 서로 다른 이름을 부르면 말이 통하지 않습니다.
이제 이름을 한 곳에 모아두고 **어느 언어로든 찾아 쓸 수 있습니다.**

| 분류 | 한국어 | 中文 | English |
|---|---|---|---|
| 전설 | 용의 심장 | 龙之心 | Dragon Heart |
| 보스 | 안타라스 | 安塔瑞斯 | Antharas |
| 서버 | 05서버 | 05服 | Server 05 |

- **아이템명 칸에서 자동완성됩니다** — 중국 혈맹원이 `龙之` 만 쳐도 그 아이템이 뜹니다
  (아이템 등록 · 연합 등록 · 연합 수정 세 곳 모두)
- ⭐ 골라 넣으면 **시트에는 언제나 한국어**가 저장됩니다. 같은 아이템이 두 이름으로
  쌓이면 나중에 개수를 세거나 찾을 수 없습니다
- **사전에 없는 이름도 그대로 칠 수 있습니다** — 자동완성은 제안일 뿐 입력을 막지 않습니다
- **보스 이름**(레이드 추가·수정)에서도 같은 자동완성이 됩니다
- 📚 용어 화면은 **[⚙️ 관리] 안에 있습니다** — 사전은 평소 들여다보는 화면이 아니라
  이름을 칠 때 뒤에서 붙는 기능이라, 홈 아이콘을 하나 더 차지하지 않게 했습니다.
  검색해서 [공유]를 누르면 `용의 심장 / 龙之心 / Dragon Heart` 가 한 줄로 복사됩니다
- **[📋 붙여넣기로 여러 개]** — 홈페이지 표를 복사해 그대로 붙여넣으면 수백 개가
  한 번에 들어갑니다. 탭·쉼표·슬래시 어느 것으로 나뉘어 있어도 받고, 한국어만
  있어도 됩니다. **이미 있는 이름은 건너뜁니다** (고쳐둔 표기가 날아가지 않게)
- 이미지 주소(구글 드라이브)를 넣으면 목록에 아이콘도 함께 보입니다
- 추가·수정·삭제는 **관리자 이상**입니다 — 표기가 틀려도 다이아는 움직이지 않고,
  실제 표기를 아는 사람이 바로 고칠 수 있어야 사전이 쓸모 있어집니다

### ⭐ 번역은 사람이 확인한 것만 씁니다

앱이 기계 번역을 하지 않습니다. **中文·English 칸은 비워도 됩니다** — 빈칸은
"아직 확인 못 했다"는 뜻이고, 그런 항목은 한국어 그대로 보여주며 목록에 `中文·English
미입력` 으로 표시됩니다. 감추면 채워진 줄과 구별이 안 됩니다.

[⚙️ 관리] → 관리 도구 → **[📚 용어 목록 채우기]** 를 실행하면 지금까지 등록한
아이템명·보스·서버 이름이 한국어로 채워집니다 (날짜·슬래시가 섞인 메모성 이름은 건너뜁니다).
中文·English 는 게임 화면을 보며 채워 넣으시면 됩니다.

**검사**: `verify:gs` 76건(신규 1건 — 시트 4함수·라우터 3종, 조회는 공개·쓰기는 관리자,
세 언어 검색, 자동완성이 **국문**을 넣는지, 사전에 없는 이름을 막지 않는지, 못 채운 표기를
감추지 않는지), E2E(신규 3건 — 중복 국문 거부, 中文으로 쳐서 고르면 국문이 입력되는지,
영문 검색과 미입력 표시). 새 검사는 일부러 코드를 깨뜨려 확인했습니다.

---

## v11.3 — 연합 인증샷을 서버별로 · 관리자도 미정산 건은 수정 · 잔액 서버 보기

> ⚠️ **`.gs` 를 붙여넣고 [배포 관리] → 새 버전으로 재배포해야 합니다.**

### 📷 연합 인증샷이 서버 줄마다 따로 붙습니다

예전에는 사진이 묶음 전체에 붙어서, 3서버 건에 3장을 올려도 **어느 서버 것인지 알 수 없었습니다.**
이제 등록 화면의 서버 줄마다 `📷 01서버 사진 추가` 버튼이 있습니다.

- [＋ 서버 추가]와 [수정]에서도 같습니다 — 그 줄에 붙습니다
- 사진이 읽어낸 인원수는 **그 줄에만** 채워집니다. 직접 고친 값은 그대로 둡니다
- 아이템을 눌러 여는 상세 화면에서도 **서버별로** 사진이 보입니다
- 정정에서 사진은 **더하기만** 됩니다 — 지우는 길은 두지 않았습니다 (증거는 되돌릴 수 없습니다)
- 🐛 v11.1 의 [＋] 로 사진을 **한 장** 붙이면 앱에서 그 사진이 통째로 안 보이던 문제도
  함께 고쳤습니다 (수식으로 저장돼 조회에서 읽히지 않았습니다)

### ✏️ 관리자도 아직 금액을 안 넣은 연합 건은 고칩니다

| | 관리자 | 마스터관리자 |
|---|---|---|
| ⏳ 미정산 건 (아이템명·서버·인원·사진) | ✅ | ✅ |
| ✅ 정산된 건 (판매금액·인원) | ❌ | ✅ |
| 삭제 | ✅ | ✅ |

정산된 건을 고치면 **혈맹운영비 잔액이 실제로 움직이므로** 마스터 전용입니다.
판정은 **시트가** 합니다 — 앱을 고치거나 주소를 직접 불러도 뚫리지 않습니다.

### 💰 잔액을 서버로 좁혀 봅니다

[💰 잔액] 위쪽에 아이템 등록 화면과 **같은 서버 칩**이 생겼습니다. 칩의 숫자가 그 서버 인원입니다.
아무것도 안 고르면 전원이 보이고, 혈맹운영비는 어느 경우에도 목록에 남습니다(합계를 봐야 하므로).

### 🏠 모든 화면 아래에 [홈] 버튼

목록을 한참 내린 뒤 위쪽 [✕]까지 다시 올라가는 것은 폰에서 멉니다.
어디까지 내렸든 손이 닿는 자리에 나가는 길을 하나 더 뒀습니다.

**검사**: `verify:gs` 75건(신규 3건 — 서버별 사진 3경로·잇기만·수식 금지, 등급 판정이
되묻기보다 앞인지·라우트가 등급을 고정하는지, 잔액 칩이 공통 컴포넌트인지·혈비가 남는지),
E2E(신규 3건 — 서버별 사진이 각 줄에 저장되는지, 관리자가 미정산은 고치고 정산된 건은
막히는지, 잔액 칩으로 좁혀도 혈맹운영비가 남는지). 새 검사는 일부러 코드를 깨뜨려
실패하는 것을 확인했습니다.

---

## v11.2.1 — 홈 화면 하나로 · 폰 뒤로가기

> 앱만 바뀝니다. **`.gs` 는 그대로**라 구글시트에 다시 붙여넣지 않아도 됩니다.

### 🏠 하단 탭바를 없애고, 모든 화면을 홈의 아이콘으로

앱을 열면 **홈**이 나옵니다. 여기서 아이콘을 눌러 화면을 열고,
**[✕] 또는 폰 뒤로가기**로 홈에 돌아옵니다.

```
💰 잔액   📦 아이템   🤝 연합
🗡️ 레이드  🙋 내 정보  📋 게시판
🌏 언어   ⚙️ 관리
```

- 아이콘 글자가 **8.8px → 13px**. 탭 7개일 때는 영문("Balance"·"Alliance")이
  잘리지 않게 그만큼 줄여야 했습니다
- **관리는 언제나 맨 마지막**입니다 — 엄지가 닿기 쉬운 자리에 두면 PIN·도구 화면이
  잘못 눌립니다
- 아이콘에는 **기다리는 건수**가 배지로 붙습니다 (미분배 · 연합 대기 · 지급할 사람 · 오늘 보스)
- 홈 위쪽에 **지금 처리할 일**이 한 줄씩 뜨고, 누르면 그 화면으로 바로 갑니다
- 🌏 **언어**를 홈에서 바로 고를 수 있습니다 ([관리] 화면의 것도 그대로 둡니다)

### ⬅️ 폰 뒤로가기가 앱을 벗어나지 않습니다

지금까지는 팝업을 닫으려고 뒤로가기를 누르면 **앱이 통째로 닫혔습니다.**
참여자를 스무 명 체크한 뒤라면 그것이 전부 사라졌습니다.

| 지금 화면 | 뒤로가기를 누르면 |
|---|---|
| 팝업(등록·정정·인증샷 크게보기) | 그 팝업만 닫힘 |
| 화면(잔액·아이템·연합·레이드…) | 홈으로 |
| 홈 | 앱을 나감 (원래대로) |

**검사**: `verify:gs` 72건(신규 2건 — 뒤로가기 표식이 언제나 한 개인지, 세 겹을 덮고
눌렀을 때 위에서부터 하나씩 닫히고 마지막에는 앱을 나갈 수 있는지 / 하단바가 없는지,
아이콘 순서와 관리가 맨 끝인지, 글자가 12px 이상인지, 모든 화면이 홈에서 열리는지),
E2E 119건(신규 4건 — 첫 화면이 홈이고 아이콘 여덟 개가 각각 그 화면을 여는지, 팝업만
닫히고 앱은 그대로인지, 화면에서 뒤로가기가 홈으로 오는지, 겹친 두 겹이 위에서부터
닫히는지, 홈의 숫자가 잔액 대시보드와 같은지). 두 검사 모두 일부러 코드를 깨뜨려
실패하는 것을 확인했습니다.

---

## v11.2 — 마스터가 분배완료 아이템도 고친다 · [＋]에 인증샷

> ⚠️ **`.gs` 를 붙여넣고 [배포 관리] → 새 버전으로 재배포해야 합니다.**

### ✏️ 분배완료 아이템의 참여 인원·분배금액 (마스터관리자)

[📦 아이템] → 아래쪽 아이템 목록 → **[👥 참여 인원·금액 고치기]**.

예전에는 [정정]으로 **금액만** 바꿀 수 있었고, 참여자를 잘못 체크했으면 삭제하고
처음부터 다시 등록해야 했습니다. 이제 한 화면에서 둘 다 고칩니다.

- 이미 나눠준 다이아를 **분배 시점 금액 그대로 회수**한 뒤 새 명단·새 금액으로
  다시 나눕니다. 회수가 **전부 끝난 뒤에** 명단을 갈아끼웁니다 —
  새 명단으로 회수하면 실제로 준 사람에게서 못 빼고 안 받은 사람에게서 뺍니다 (규칙 2-1)
- 혈맹운영비도 함께 맞춰집니다. **불변식은 그대로**입니다: `혈비 + Σ각자 = 판매금액`
- 참여자를 바꾸면 참여횟수는 등록 이력 전체를 **다시 셉니다** (규칙 3)
- 돈이 움직이므로 **바뀔 숫자를 보여준 뒤에만** 실행됩니다 (규칙 5-1)
- 이미 **지급✓** 된 사람이 있으면 시트가 아예 막습니다 (회수할 잔액이 없으므로)

### ➕ 참여 서버 추가에도 인증샷

[＋] 화면에서도 사진을 붙일 수 있습니다. **묶음의 인증샷에 이어 붙습니다** —
덮어쓰면 처음 등록할 때 넣은 사진이 사라집니다.

사진을 넣으면 인원수를 읽어주지만, **등록 화면과 똑같은 규칙**입니다:
숫자를 한 번이라도 직접 고치면 사진 결과가 덮어쓰지 않고, 서버가 여럿이면
자동으로 넣지 않고 읽은 값만 보여줍니다.

**검사**: `verify:gs` 70건(기존 검사 확장 — 명단 교체가 회수보다 **뒤**인지,
분배완료 건이 확인을 요구하는지, 공통 되돌리기 코어를 쓰는지),
E2E 115건(신규 1건 — 3만을 둘이 나눈 뒤 6만·세 명으로 정정해서 각자 잔액과
혈맹운영비가 정확히 맞는지, 되묻는 단계에서는 아무것도 안 바뀌는지).
두 검사 모두 일부러 코드를 깨뜨려 실패하는 것을 확인했습니다.

---

## v11.1 — 마스터 전권 정정 · 인증샷 보기 · 팝업 닫기 버튼 · 서버 표기 버그 수정

> ⚠️ **`.gs` 를 붙여넣고 [배포 관리] → 새 버전으로 재배포해야 합니다.**

v11.0 을 붙여넣은 **실제 시트에서 바로 나온 문제**입니다.

시트에 `01` 을 써넣어도 셀 서식이 자동이면 구글시트가 **숫자 1 로 바꿔** 저장합니다.
읽을 때는 `1` 로 돌아오는데, 서버별 누적은 `01`~`12` 로만 집계하므로 **그 건의 금액이
누적에서 통째로 빠집니다.** 행에는 그대로 남아 있어서 "왜 합계가 안 맞지"만 남고
원인을 찾을 수 없는 종류의 버그입니다.

- `_normServer` 로 **읽을 때 맞춥니다.** 이미 `1` 로 저장된 옛 행도 제대로 집계되므로
  **시트를 손댈 필요가 없습니다**
- 등록도 `5` 를 받아 `05` 로 저장하고, 쓰기 **전에** 서버 칸을 글자 서식으로 바꿔
  애초에 숫자가 되지 않게 합니다 — 쓴 뒤에 서식을 바꿔봐야 이미 `1` 이 된 값은
  돌아오지 않습니다
- 앱의 `normServer`(`lib/client.ts`)와 **같은 규칙**입니다. `A1` 처럼 알아볼 수 없는
  값은 지어내지 않고 그대로 둡니다 (규칙 7)

### 📷 인증샷을 앱 안에서 바로 본다

예전에는 `📷 1` 링크뿐이라 누르면 구글 드라이브가 **새 탭으로** 열렸습니다.
폰에서는 앱이 통째로 가려지고, 돌아오면 보던 자리를 잃습니다.

- **[📦 아이템] 아이템명을 누르면** 참여자 명단과 인증샷이 함께 열립니다
- **[🤝 연합] 아이템명·아랫줄을 누르면** 서버별 인원과 인증샷이 열립니다
- 썸네일을 누르면 **앱 안에서** 화면 가득 뜹니다. [원본 열기]도 남겨뒀습니다
- 시트에 저장되는 값은 드라이브 **뷰어 페이지** 주소라 `<img>` 에 그대로 넣으면
  아무것도 안 나옵니다. `photoView` 가 썸네일 주소로 바꿉니다.
  드라이브 링크가 아니면 손대지 않습니다 — 관리자가 손으로 붙여넣은 다른 주소를
  깨뜨리면 안 됩니다 (규칙 7)
- **못 불러온 사진을 조용히 숨기지 않습니다.** 자리와 함께 "안 열림"을 보여주고
  원본 링크를 남깁니다 — 숨기면 사진을 안 붙인 줄로 압니다

### ✕ 팝업이 바깥을 눌러도 닫히지 않는다

폰에서는 팝업이 화면을 거의 다 채웁니다. 스크롤하려다 가장자리를 스치면 그대로
닫혔고, **입력하던 내용이 통째로 사라졌습니다.** 참여자를 스무 명 체크한 뒤라면
손해가 큽니다.

- 닫는 길은 **오른쪽 위 [✕]** 와 아래쪽 버튼(취소·저장·닫기) 두 가지뿐입니다
- [✕]는 스크롤을 따라와 긴 팝업에서도 늘 같은 자리에 있습니다
- Esc 는 그대로 둡니다 (PC 에서 실수로 눌릴 일이 없고, 키보드만 쓰는 사람에게 필요합니다)

### 🐛 사진이 읽은 인원수가 손으로 넣은 값을 덮어쓰던 문제

**사용자가 실제로 겪은 사고입니다.** 연합 아이템에 스크린샷 3장을 붙이고 인원을
**13 · 8 · 8** 로 고쳐 넣었는데, 저장된 값은 **8 · 8 · 8** 이었습니다.

원인: 사진 분석이 끝날 때마다 읽어낸 인원수를 **첫 줄에 무조건 써넣고** 있었습니다.
3장을 한꺼번에 고르면 세 번 덮어써서 마지막 사진의 값만 남습니다.

- 인원 칸을 **한 번이라도 직접 고치면**, 그 뒤로는 사진 결과가 **절대 덮어쓰지 않습니다**
- 자동으로 채우는 것은 **서버가 한 줄뿐이고 아직 아무도 손대지 않은** 경우뿐입니다.
  어느 서버의 사진인지는 시스템이 알 수 없으므로, 여러 줄에 흩뿌리면 틀린 값이 박힙니다 (규칙 7)
- 대신 각 사진이 몇 명으로 읽었는지 `📷1 13명 · 📷2 8명 · 📷3 8명` 으로 **보여주기만** 합니다.
  넣는 것은 사람이 합니다

### ✏️ 마스터관리자는 연합 항목을 전부 고친다

아이템 탭과 같은 자리에 [수정]이 생겼습니다. **등록만 된 것도, 정산까지 끝난 것도**
고칠 수 있습니다.

| | 고칠 수 있는 것 |
|---|---|
| ⏳ 금액 대기 중 | 아이템명 · 서버별 인원 |
| ✅ 정산 완료 | 아이템명 · 서버별 인원 · **판매금액** |

정산된 건을 고치면 서버별 몫이 다시 계산되고 혈맹운영비 적립액도 함께 조정됩니다.
**전액을 다시 더하지 않고 차액만** 더합니다 — 전액으로 하면 고칠 때마다 운영비가
불어납니다. 돈이 움직이므로 **바뀔 숫자(혈비 얼마 → 얼마)를 보여준 뒤에만** 실행됩니다
(규칙 5-1). 삭제는 예전처럼 그대로이며 적립분을 회수합니다.

### ➕ 관리자는 참여 서버를 추가한다

[금액 넣기] 왼쪽에 **[＋]** 가 생겼습니다. 레이드가 끝난 뒤 "우리 서버도 갔었다"는
이야기가 늦게 오는 일이 흔한데, 그때마다 마스터를 불러야 하면 등록이 미뤄집니다.

- **줄을 더하기만 합니다.** 이미 있는 서버·인원·아이템명은 이 경로로 **고칠 수 없습니다**
  — 시트가 막습니다. 화면을 고쳐도 뚫리지 않습니다
- 이미 들어 있는 서버는 다시 못 넣습니다 (두 줄이 되면 인원이 갈려 비율이 틀어집니다)
- 이미 정산된 건에는 못 더합니다 — 이미 나눠준 몫과 어긋나므로 정정을 써야 합니다

### 📊 서버별 누적에 대기 인원이 보인다

등록만 하고 금액을 안 넣은 건은 누적에 들어가지 않습니다(0원짜리가 건수만 부풀리므로).
그런데 8명을 넣고 나서 `인원 0` 만 보이면 **입력이 안 된 줄 압니다.** 이제
`⏳ 대기 8명 (1건)` 이 함께 나옵니다. 누적 금액에는 섞지 않습니다.
아무 일도 없었던 서버 줄은 접어둡니다 — 12줄 중 열 줄이 0이면 읽을 것이 없습니다.

**검사**: `verify:gs` 69건(신규 4건 — ① 서버 표기 맞춤 11케이스를 앱·시트 두 구현으로
대조하고 읽는 자리 2곳이 모두 거치는지와 서식을 값보다 **먼저** 바꾸는지, ② 인증샷
주소 변환 4케이스와 실패 표시·원본 링크·두 화면 연결, ③ 배경 닫기가 사라졌고 [✕]가
스크롤을 따라오는지, ④ 정정이 마스터 전용이고 확인을 요구하며 혈비를 **차액만** 조정하는지 · 서버 추가가 기존 줄을 못 고치는지, ⑤ 사진 결과가 사람이 넣은 값을 덮어쓰지 않는지), E2E 114건(신규 6건 — 그중 하나는 위 사고를 그대로 재현합니다: 사진 3장을 붙이고 13·8·8 을 넣은 뒤 저장까지 해서 값이 살아남는지). 새 검사는 전부 일부러 코드를 깨뜨려
실패하는 것을 확인했습니다.

---

## v11.0 — 연합: 아이템 하나에 여러 서버 · 사진 여러 장 · 미분배 아이템 수정

> ⚠️ **`.gs` 를 붙여넣고 [배포 관리] → 새 버전으로 재배포해야 합니다.**
> ([새 배포]로 하면 URL 이 바뀝니다.)

### ① 연합 — 아이템 하나에 참여 서버를 여러 곳

예전에는 **서버 하나당 등록을 따로** 해야 했습니다. 같은 아이템이 3서버에 걸치면
등록 세 번, 금액 넣기 세 번이고, 그중 하나를 빠뜨려도 아무도 알아채지 못했습니다.

이제 한 번에 넣습니다.

```
아이템명   연합 보스
참여 서버  01서버 10명 · 02서버 5명 · 05서버 3명
인증샷     📷 1  📷 2  📷 3   (선택 — 없어도 등록됩니다)
```

시트에는 여전히 **서버마다 한 줄**이 들어가고, 새 **K열 "묶음"** 값으로 한 건이
됩니다. 앱은 언제나 묶음 단위(=아이템 하나)로 보여줍니다.
아이템명을 누르면 어느 서버가 몇 명 참여했고 얼마를 받았는지 펼쳐집니다.

### ② 정산 산식이 비중(%)에서 **인원수 비례**로

아이템 분배와 같은 모양입니다.

```
혈비     = floor(판매금액 × 10%)
분배가능 = 판매금액 − 혈비
서버별   = floor(분배가능 × 그 서버 인원 ÷ 총 인원)
잔여     = 분배가능 − Σ서버별      → 전액 혈비로 추가 귀속
```

**불변식: `혈비 + Σ서버별 = 판매금액`** — 다이아는 사라지지도 생겨나지도 않습니다.
잔여를 특정 서버에 얹지 않습니다 (규칙 2). 예: 10만을 01서버 10명 · 02서버 5명이
나누면 → 혈비 1만 · 01서버 6만 · 02서버 3만.

**이 혈비는 [잔액현황]의 혈맹운영비 잔액에 실제로 적립됩니다** (새 L열 "혈비"에 기록).
기록을 삭제하면 적립분도 함께 회수합니다 — 안 되돌리면 지운 기록의 혈비만 잔액에
남아 장부가 맞지 않습니다. 개인 잔액은 어느 단계에서도 바뀌지 않습니다.

옛 기록(v10.9 이하)은 그대로 읽힙니다. '묶음'이 빈 행은 **그 한 줄이 한 건**이고,
비중(%) 열은 옛 값을 보기 위해 자리만 남겨둡니다.

### ③ 인증샷 여러 장 — 연합·아이템 둘 다

레이드 참여자가 한 화면에 안 들어가 두세 장으로 나눠 찍는 일이 흔한데,
예전에는 **마지막 한 장만** 남았습니다. 이제 한꺼번에 고를 수 있고,
장마다 OCR 이 찾은 사람이 **계속 더해집니다** (덮어쓰지 않습니다).

시트 저장 방식: 한 장이면 예전처럼 `=HYPERLINK(...)`, 두 장 이상이면 URL 을
줄바꿈으로 나열합니다 — `HYPERLINK` 은 링크를 한 개만 담기 때문입니다.
읽는 쪽은 두 형태를 하나로 봅니다. 시즌종료·데이터 이관에서도 그대로 옮겨갑니다.

### ④ ✏️ 미분배 아이템 수정 (마스터관리자)

`⏳미분배` 아이템의 **아이템명과 참여자**를 고칩니다. 등록할 때 사람을 잘못
체크했으면 지우고 다시 만들 필요 없이 그 자리에서 고칩니다.

- 참여자를 바꾸면 참여횟수는 **등록 이력 전체를 다시 세어** 맞춥니다.
  증감으로 맞추면 한 번만 어긋나도 영원히 틀어진 채로 남습니다 (규칙 3)
- 이미 `✅분배완료` 된 아이템은 **시트가 거부합니다** — 그쪽은 금액을 회수했다가
  다시 나눠줘야 해서 [정정]이 담당합니다
- 미분배라 다이아는 아직 아무에게도 안 갔습니다. 잔액은 건드리지 않습니다

**검사**: `verify:gs` 65건 (신규 4건 — ① 연합 산식을 앱·시트 두 구현으로 5,007건
무작위 대조하며 보존 불변식과 잔여 범위까지 확인, ② 중복 서버 거부·인증샷 선택·
혈비 적립/회수·참여횟수 무관·라우트와 앱이 묶음 단위인지, ③ 인증샷 읽기 4케이스와
앱 두 화면의 여러 장 지원, ④ 아이템 수정이 분배 후를 거부하고 마스터 전용인지),
E2E 108건(신규 3건 — 여러 서버 등록부터 정산·중복 거부·혈맹운영비 적립과 회수까지 한 흐름으로, 아이템명을 눌러 서버별 인원이 펼쳐지는지, 아이템 수정이 관리자에게 막히고 분배 후에는 거부되는지). 모의 시트의 연합·아이템 수정 처리도 같은 산식·같은 결과 코드로 맞췄습니다.

---

## v10.9.2 — 사람 목록을 어디서나 이름순(ㄱ~ㅎ)으로 (앱만, 시트 재배포 불필요)

인원이 늘면서 목록에서 한 사람을 눈으로 찾기 어려워졌습니다. 게다가 화면마다
순서가 달라서(잔액은 금액순, 관리는 시트에 넣은 순) 같은 명단인데 자리가 매번
바뀌었습니다.

| 화면 | 전 | 후 |
|---|---|---|
| **잔액** | 분배전 많은 순 | **이름순** (혈맹운영비만 맨 위 고정) |
| **아이템** 참여자 칩 | 시트에 넣은 순 | **이름순** |
| **관리** 혈맹원 명단 | 시트에 넣은 순 | **이름순** (혈맹운영비만 맨 위 고정) |
| **[⏪ 이전 아이디에서 불러오기]** 후보 | 금액 많은 순 | **이름순** |

- **혈맹운영비는 언제나 맨 위**입니다. 정렬을 **먼저** 하고 그다음에 올립니다 —
  순서를 바꾸면 정렬이 혈비를 다시 아래로 내려버립니다
- `Intl.Collator('ko')` 를 씁니다. 코드포인트로 비교하면 `ㄱ~ㅎ` 이 맞지 않고
  라틴·한자가 섞였을 때 순서가 뒤집힙니다
- `유저2` 가 `유저10` 보다 앞에 옵니다 (글자로만 비교하면 뒤집힙니다)
- `잠단 (斬斷)` 과 `잠단(斬斷)` 이 같은 자리에 옵니다 (`_normName` 경유, 규칙 4)

> 잔액 탭이 금액순이 아니게 됐지만, **[받을 잔액이 남은 사람만 보기]** 와 이름 검색이
> 있어 지급할 때도 불편하지 않습니다. 오히려 분배할 때마다 자리가 바뀌지 않습니다.

**검사**: `verify:gs` 61건(신규 1건 — 정렬을 실제로 실행해 4케이스, 화면 3곳이 모두
같은 함수를 쓰는지, 옛 금액순이 남아 있지 않은지, 혈비를 정렬 **뒤**에 올리는지),
E2E 105건(신규 1건 — 잔액·아이템·관리·가져오기 후보 네 목록을 실제 화면에서 읽어
한국어 정렬과 대조).

---

## v10.9.1 — 이전 아이디에서 기록 가져오기 + 추가 버튼 하나로 (앱만, 시트 재배포 불필요)

**① [⏪ 이전 아이디에서 불러오기]** — 혈맹원 관리 화면에 생겼습니다.

게임에서 아이디를 바꾼 사람을 **먼저 새 아이디로 넣어두고**, 나중에 그 사람 화면에서
옛 아이디를 골라 기록을 가져올 수 있습니다. 분배전·분배완료·참여횟수가 넘어옵니다.

- 후보마다 **따라올 금액을 함께** 보여줍니다 — 이걸 보고 같은 사람인지 판단합니다
- **자기 자신과 혈맹운영비는 후보에 없습니다.** 자신을 고르는 것은 뜻이 없고,
  혈비는 사람이 아니라 길드의 금고라 누구에게도 합쳐지면 안 됩니다
- 고르면 **서버가 양쪽 금액을 보여주며 한 번 더 묻습니다.** 앱이 확인값을 임의로
  채우지 않습니다 — 그러면 안전장치가 통째로 무력화됩니다

**② 아이디 칸으로는 더 이상 합칠 수 없습니다**

예전에는 아이디 칸에 남의 이름을 치면 곧바로 "합칠까요?" 가 떴습니다. 개명하려던
관리자가 **오타 하나로 두 사람 잔액을 합쳐버리기 좋은 자리**였습니다.
이제 막고, 위의 전용 버튼을 가리킵니다.

**③ [혈맹원 추가] 하나로 통합**

[혈맹원 추가]와 [명단 일괄 추가]가 같은 일을 했습니다. 버튼이 둘이면 무엇이 다른지
묻게 됩니다. 이제 **[혈맹원 추가] 하나**이고, 그 안에서 한 명이든 여럿이든
직접 입력·사진 모두 됩니다.

한 명 추가도 이제 **넣기 전에 판정을 거칩니다** — 이미 있는 사람인지, 개명으로
이어야 할 사람인지 먼저 보여줍니다. 예전 한 명 추가 화면에는 그 검사가 없었습니다.

**검사**: `verify:gs` 60건(신규 1건 — 중복 아이디 차단, 후보에서 본인·혈비 제외,
되묻기 유지, 추가 입구 1개), E2E 104건(신규 2건 — 한 명 추가가 통합 화면으로 되는지 /
중복 아이디가 막히고, 후보에 본인·혈비가 없고, 되물은 뒤 분배전·분배완료·참여횟수가
넘어오며 같은 이름이 한 줄만 남는지).

---

## v10.9 — 명단 사진의 [혈맹·서버] 표시를 이름에서 떼어낸다

> 🔧 **이 버전은 `.gs` 를 바꿉니다.** Apps Script 편집기에 새 `.gs` 전체를 붙여넣고
> **[배포] → [배포 관리] → 편집(✏️) → 새 버전** 으로 재배포해주세요.
> (❌ [새 배포] 로 하면 URL이 바뀝니다.)

게임 명단을 찍으면 한 줄이 `참관K[어레02] +` 처럼 나옵니다. 대괄호 안은 혈맹·서버
표시이고 `+` 는 친구추가 버튼입니다 — **이름이 아닙니다.**

그대로 등록하면 멤버DB에 `참관K[어레02] +` 가 들어가고, 이후 아이템 인증샷 OCR 은
게임에서 `참관K` 로 읽으므로 **그 사람만 영원히 매칭되지 않습니다.** 실제로 사용자가
등록 직전 화면에서 발견했습니다.

- **판정 전에 떼어냅니다** (`_stripNameTag`). 서버는 그 화면 위에서 따로 고릅니다
- ★ **소괄호는 절대 건드리지 않습니다** — `잠단 (斬斷)` 의 한자 표기가 사라집니다
- ★ 떼어내야만 기존 멤버와 이어집니다. `가이[어레02] +` 는 이제 `가이` 로 인식돼
  **'이미 있음'** 이 됩니다. 안 떼면 신규가 되어 잔액이 0부터 다시 시작합니다
- ★ 여는 대괄호가 OCR 에서 떨어져 나간 꼬리(`노왕계색마재 어레02]`)는 뒤 토막만
  버립니다. **그 토막이 첫 토막이면 손대지 않습니다** — 지우면 이름 쪽을 지웁니다.
  `곡중인산K02] 관` 처럼 알아볼 수 없는 줄은 **'확인 필요'** 로 남깁니다 (규칙 7)
- 무엇을 떼어냈는지 **원문을 화면에 같이 보여줍니다** — 조용히 바뀌면 알 수 없습니다
- 서버 지정이 드롭다운 → 칩으로 바뀌었습니다 (다른 화면과 같은 모양)

### 안내 문구를 바로잡았습니다

**"개명하면 지난 시즌 기록도 새 이름으로 넘어갑니다"** 라고 적혀 있었는데 **사실이
아니었습니다.** `시즌1`·`시즌2` 시트는 그 시점에 얼어붙은 기록이고 개명은 건드리지
않습니다 — 지난 기록을 나중에 고쳐 쓰면 "기록은 지워지지 않는다"는 이 시스템의
약속이 깨집니다. 동작이 맞고 **문구가 틀렸던 것**이라, 문구를 고쳤습니다.
(분배전·분배완료·참여횟수는 예전대로 그대로 승계됩니다.)

### 개명 병합이 멤버DB에 중복 행을 남기던 버그를 고쳤습니다

**"먼저 신규로 넣어두고 나중에 옛 아이디에서 불러오는"** 흐름에서 늘 생기던 문제입니다.

옛 `_renameCore` 는 옛 행의 **이름만** 새 이름으로 덮어썼습니다. 바꿀 이름이 이미
멤버DB에 있으면 **같은 이름이 두 줄** 남아, 명단과 아이템 참여자 칩에 한 사람이
두 번 보였습니다.

- **잔액은 예전에도 정상적으로 합쳐졌습니다** — 금액이 틀어진 적은 없습니다
  (분배전·분배완료·참여횟수 모두 합산, 잔액현황의 옛 행은 삭제)
- 이제 **이미 있던 행만 살리고 옛 행은 지웁니다**
- ★ 살아남는 행의 **빈 칸만** 옛 행에서 채워 옵니다 (게임표시명·서버·한자표기).
  채워져 있는 값을 덮어쓰면 관리자가 방금 넣은 값을 지우게 됩니다
- 분배비중은 옮기지 않습니다 — 항상 값이 있어 "비어 있으면 채운다"가 성립하지 않습니다
- 되묻기는 그대로입니다 — 합치기 전에 **양쪽 분배전 금액을 보여주고** 다시 묻습니다

**검사**: `verify:gs` 59건(신규 2건 — 떼어내기 11케이스를 **실제 `.gs` 를 실행해**
확인하고 모의 시트가 같은 결과를 내는지 대조, '확인 필요' 처리, 원문 표시, 서버 칩 /
기존 1건 확장 — 버전을 **다섯 곳**에서 대조하도록 모의 시트를 포함),
E2E 102건(신규 2건 — 사진에서 나오는 5가지 모양이 각각 어떻게 판정되는지 /
신규로 넣은 뒤 옛 아이디에서 불러왔을 때 분배전·분배완료·참여횟수가 넘어오고
같은 이름이 한 줄만 남는지).

---

## v10.8.9 — [관리]의 서버 표기도 같은 배지로 (앱만, 시트 재배포 불필요)

v10.8.8 에서 잔액·아이템·지급·분배·내 정보를 맞췄는데 **[관리] 탭만 남아 있었습니다.**
거기서는 이름 아랫줄에 `01 서버 · 분배전 12,400 다이아` 처럼 **글로** 적혀 있었습니다.

| | 전 | 후 |
|---|---|---|
| 혈맹원 명단 | 아랫줄에 `01 서버 · …` | 이름 앞에 `01` 배지 (다른 화면과 같은 모양) |
| 서버 일괄 지정 | 오른쪽에 흐린 글씨 `01` | 같은 `01` 배지 |

- 아랫줄의 `01 서버 ·` 글은 **뺐습니다.** 배지와 겹쳐 같은 정보가 두 번 나옵니다
- ★ **미지정에는 배지를 만들지 않습니다.** 빈 배지는 지정된 것처럼 보입니다.
  일괄 지정 목록에서는 예전처럼 흐린 `미지정` 글씨로 둡니다
- 알아볼 수 없는 값(`1서버`·`A1`)의 ⚠️ 배지를 위해 `--danger-soft` 색을
  밝은·어두운 화면 양쪽에 새로 정했습니다 — 없는 색을 쓰면 배경이 엉뚱하게 나옵니다

**검사**: `verify:gs` 57건(기존 검사 확장 — 명단·일괄 지정의 배지, 아랫줄에 글이
남아 있지 않은지, 미지정에 배지를 만들지 않는지, 두 화면 모두에 `--danger-soft` 가 있는지),
E2E 100건(기존 2건 보강 — 명단의 `01` 배지와 아랫줄에 '서버' 글자가 없는지,
일괄 지정에서 `'2'` 가 `02` 배지로 뜨고 미지정에는 배지가 없는지).

---

## v10.8.8 — 서버 번호를 누구인지 확인하는 모든 자리에 (앱만, 시트 재배포 불필요)

[잔액]에는 이름 앞에 서버 번호가 붙어 있었는데 다른 화면에는 없었습니다. **같은 사람이
화면마다 다르게 보이니**, 서버가 갈린 뒤로는 비슷한 이름을 이름만으로 가릴 수 없었습니다.

이제 **누구인지 확인해야 하는 자리는 전부** 같은 표기를 씁니다.

| 화면 | 모양 |
|---|---|
| **아이템 참여자 칩** | `01` 배지 + 이름 (잔액 목록과 같은 배지) |
| **등록 확인 화면** | `01 가이, 02 잠단 (斬斷)` |
| **지급 창** | `💰 01 TC무식 (车武植) 지급` |
| **분배 확인 화면** | `⚖️ 04 대서과Z (大西瓜Z) (50%)` |
| **내 정보 이름 목록** | `01 가이` — 남의 이름을 고르면 남의 잔액을 보게 됩니다 |

- 표기 규칙은 **한 벌**(`personLabel`)입니다. 화면마다 따로 조립하면 또 어긋납니다
- ★ **서버가 없으면 아무것도 붙이지 않습니다.** 빈 자리나 `--` 를 넣으면 지정된 사람과
  아닌 사람이 같아 보입니다
- ★ 참여자 칩의 배지가 먹는 폭은 **이름 크기 계산에서 미리 뺍니다.** 공짜로 얹으면
  그만큼 이름이 삐져나가고, 잘린 이름은 다른 사람으로 오인됩니다
- 체크된 칩(초록 바탕)에서는 배지 색을 뒤집습니다 — 안 그러면 묻혀서 안 보입니다

**검사**: `verify:gs` 57건(신규 1건 — 규칙이 한 벌인지, 화면 4곳이 직접 조립하지 않는지,
서버가 없을 때 자리를 만들지 않는지, 배지까지 포함한 폭 예산 4케이스를 실제로 계산),
E2E 100건(신규 1건 + 기존 2건 보강 — 칩 배지가 `01`/`02`로 뜨고 미지정자에겐 안 뜨는지,
배지가 붙어도 이름이 칩 밖으로 안 나가는지, 지급 창이 `01 이름 (한자)` 모양인지).

---

## v10.8.7 — 혈맹운영비를 맨 위로 + `1` 과 `01` 을 같은 서버로 (앱만, 시트 재배포 불필요)

**① 혈맹운영비가 [잔액]·[혈맹원 관리] 맨 위에 옵니다**

혈비는 사람이 아니라 **길드의 금고**입니다. 사람들 사이에 섞여 있으면 인원이 늘수록
아래로 밀려 매번 찾아 내려가야 했습니다.

- 맨 윗줄에 `운영비` 배지를 붙였습니다. 표시가 없으면 "잔액이 제일 많은 사람"으로 읽힙니다
- **나머지 순서(잔액 많은 순)는 그대로**입니다 — 혈비만 올리고 정렬은 건드리지 않습니다
- ★ **검색·필터에 걸려 빠진 것을 억지로 되살리지는 않습니다.** "받을 사람만 보기"를
  켰는데 혈비가 튀어나오면 필터가 거짓말을 하는 것입니다

**② `01 서버 0명` 버그를 고쳤습니다** (사용자 발견)

시트의 서버 칸은 사람이 손으로 넣는 자리라 `1` 과 `01` 이 섞입니다.
예전에는 **[잔액] 탭만** 앞의 0 을 채워 보여주고 [아이템] 탭은 그러지 않았습니다.
그래서 `1` 로 저장된 사람이 잔액에서는 `01` 로 보이는데, 아이템의 `01` 칩에는
안 잡혀 **"01 서버 0명"** 이 나왔습니다. 그 사람은 서버로 걸러도 어디에도 안 잡힙니다.

- 서버 값을 읽는 **모든 화면이 `normServer` 한 곳을 지나갑니다**
  ([잔액] 배지 · [아이템] 칩과 인원수 · [혈맹원 관리] 칩 · [서버 일괄 지정] 표시와 접기)
- ★ **숫자로 읽히는 것만 맞춥니다.** `1서버`·`A1` 처럼 알아볼 수 없는 값은 손대지 않고
  그대로 두고 ⚠️ 로 표시합니다 — 그럴듯하게 바꾸면 엉뚱한 서버로 분류됩니다 (규칙 7)
- **시트를 고치지 않아도 됩니다.** `1` 로 남아 있어도 앱이 알아서 `01` 로 읽습니다

**검사**: `verify:gs` 56건(신규 2건 — 표기 맞춤 11케이스를 실제로 실행, 화면 3곳이
개별 `padStart` 를 하지 않는지 / 혈비 올리기 순서 보존 4케이스, 정렬 뒤에 올리는지, 배지),
E2E 99건(신규 1건 + 기존 2건 보강 — `'2'` 로 저장된 사람이 잔액에서 `02` 로 보이고
아이템의 `02` 칩에 2명으로 잡히며 일괄 지정에서 경고가 안 붙는지, 혈비가 맨 위이되
검색 결과에는 안 끼어드는지).

---

## v10.8.6 — 아이템 등록을 서버로 좁힌다 (앱만, 시트 재배포 불필요)

혈원이 늘면서 참여자 칩이 40개를 넘어갔습니다. 한 서버짜리 레이드를 등록하는데
전 서버 인원을 훑어야 하니 체크를 빠뜨리거나 잘못 누르기 쉬웠습니다.

**등록 화면에서 서버를 고르면 그 서버 사람만 보여줍니다.**

- 칩에 **서버별 인원 수**가 같이 나옵니다 (`01 2` · `02 2`). 몇 명짜리인지 모르면
  무엇을 고를지 판단할 수 없습니다
- **여러 개 고를 수 있습니다** — "더 추가하실 서버는 없나요?" 두세 서버가 같이 들어간
  레이드가 있기 때문입니다
- 인원이 없는 서버는 접힙니다 (`+ 다른 서버 7개`). [혈맹원 관리]와 **같은 접기 규칙**을
  씁니다 — 규칙이 두 벌이 되면 화면마다 다르게 접힙니다

### 안전장치 세 가지

**① 체크한 사람은 서버와 무관하게 언제나 보입니다** — 이게 제일 중요합니다.

사진에서 자동으로 찾아낸 참여자가 다른 서버라는 이유로 화면에서 사라지면,
관리자는 그 사람이 빠진 줄 알고 등록합니다. **실제로는 들어가 있으므로 확인 화면과
결과가 어긋납니다.** 서버를 갈아타도 이미 체크한 사람은 목록에 남습니다.

**② 나머지는 감추는 것이 아니라 접어둡니다** — `+ 나머지 N명도 보기`.
갑자기 다른 서버 사람이 낀 레이드에서 고를 길이 없으면 안 됩니다.

**③ 아무 서버도 안 고르면 예전 그대로 전원이 나옵니다.**
서버 칸이 아직 비어 있어도 등록이 막히지 않습니다. `서버 미지정` 칩도 따로 있습니다.

그리고 [전체 선택]·[전체 해제]는 **지금 보이는 사람에게만** 적용됩니다.
서버로 좁혀 놓고 눌렀는데 안 보이는 사람까지 딸려 들어가면 좁힌 의미가 없습니다.

**검사**: `verify:gs` 54건(신규 1건 — 체크 유지 · 미선택 시 전원 · 접어두기 ·
전체선택 범위 · 미지정 칩 · 접기 규칙을 실제로 실행해 5케이스),
E2E 98건(신규 1건 — 칩 인원 수 · 복수 선택 · **서버를 갈아타도 체크한 4명이 남는지** ·
펼치기/접기 · 좁힌 채 등록한 인원이 시트까지 그대로 · 등록 뒤 필터 초기화).

---

## v10.8.5 — 서버 지정: 드롭다운 대신 칩 + 한 화면에서 일괄 지정 (앱만, 시트 재배포 불필요)

혈원이 늘면서 서버가 01~12 로 나뉘었습니다. 앞으로 **아이템 등록을 서버로 좁히려면**
멤버DB 의 서버 칸이 채워져 있어야 하는데, 지금은 대부분 비어 있습니다.
그 준비 작업을 먼저 했습니다. (아이템 등록 화면 자체는 다음 단계입니다.)

**① 서버 고르기가 드롭다운에서 칩으로 바뀌었습니다**

`<select>` 는 열고 · 굴리고 · 누르는 **세 동작**입니다. 40명에게 서버를 넣으면 120번입니다.
칩은 한 번 누르면 끝입니다.

- **실제로 인원이 있는 서버만 앞에 두고 나머지는 접습니다.** 12개 중 넷만 쓰는 길드에서
  나머지 여덟 개는 매번 눈에 걸리는 잡음입니다. `+ 다른 서버 N개` 를 누르면 전부 펼쳐집니다
- 다만 **아직 아무도 배정되지 않았을 때는 접지 않습니다** — 접으면 고를 것이 없어집니다
- 지금 고른 값은 접힌 쪽에 있어도 **항상 보입니다.** 안 보이면 뭘 골랐는지 알 수 없습니다

**② [🗂️ 서버 일괄 지정] — 서버 하나 고르고, 사람 여럿 체크하고, 한 번에**

[혈맹원 관리]를 40번 열었다 닫는 것은 현실적이지 않습니다. 한 화면에서 끝냅니다.

- 기본은 **서버가 비어 있는 사람만** 보여줍니다. 이미 넣은 사람까지 훑을 이유가 없습니다
- **혈맹운영비는 목록에 없습니다** — 사람이 아니라 계정이라 서버를 붙일 대상이 아닙니다
- 형식이 어긋난 값(`1` 처럼 앞의 0 이 빠진 것)은 **⚠️ 붉게** 표시합니다.
  이대로 두면 나중에 서버로 걸러도 그 사람만 안 잡힙니다
- 시트에 한 번에 보내는 액션이 없어서 **한 명씩 차례로** 저장합니다. 그래서
  ① 대상별 개별 try/catch — 한 명이 실패해도 나머지는 계속 갑니다
  ② 끝나면 **실패한 사람의 이름까지** 알려줍니다. 숫자만 주면 누구를 다시 해야 하는지 모릅니다
- 관리 탭 상단에 `서버가 비어 있는 혈맹원이 N명입니다` 안내를 띄웁니다

**검사**: `verify:gs` 53건(신규 1건: 드롭다운 제거 · 접기 안전 · 혈비 제외 · 개별 try/catch ·
실패 이름 보고 · 문구 14개), E2E 97건(신규 1건: 칩 접기/펼치기 · 미지정 2명 · 혈비 제외 ·
05 서버로 2명 일괄 지정이 시트까지 반영 · 기존 배정은 그대로).

---

## v10.8.4 — 아이콘이 폰에 실제로 반영되게 (앱만, 시트 재배포 불필요)

아이콘을 v10.8.3 에서 갈았는데 **폰에는 옛 그림이 그대로** 나왔습니다. 원인 두 가지를 잡았습니다.

**① 서비스워커가 옛 아이콘을 영원히 붙들고 있었습니다**

JS·CSS 는 파일명에 빌드 해시가 붙어서 캐시 우선으로 둬도 낡을 일이 없습니다.
그런데 **아이콘과 manifest 는 주소가 늘 같습니다.** 한 번 캐시에 들어가면 파일을 바꿔도
영원히 옛 그림이 나옵니다.

- 캐시 이름을 `guild-shell-v2` 로 올려 옛 캐시를 버립니다
- 주소가 고정된 것(`manifest.webmanifest` · `favicon.ico` · `icon*.png` · `apple-icon*.png`)만
  **네트워크 우선**으로 바꿨습니다. 아이콘은 설치할 때나 탭을 열 때만 읽히므로
  왕복이 늘어도 부담이 없고, 오프라인이면 캐시로 물러섭니다
- 검사가 이 규칙을 강제합니다 — 다음에 아이콘을 갈 때 같은 일이 반복되지 않습니다

**② `favicon.ico` 가 아예 없었습니다**

브라우저 탭·즐겨찾기·바탕화면 바로가기는 `/favicon.ico` 를 먼저 찾습니다. 없었습니다.

- 16·32·48px 세 크기를 담은 `app/favicon.ico` 를 만들었습니다
- ★ **작은 크기에는 얼굴 쪽을 크게 잡은 crop** 을 씁니다. 앱 아이콘과 똑같이 잘라 넣으면
  16px 에서 얼굴이 몇 픽셀밖에 안 돼 초록 얼룩이 됩니다. 같은 그림이지만 크기에 맞는 구도입니다

> 📱 **홈 화면에 이미 설치하신 경우**, 안드로이드·iOS 는 설치 시점의 아이콘을 저장합니다.
> 아이콘을 바꾸려면 **삭제 후 다시 추가**해야 합니다.

---

## v10.8.3 — 마스코트 아이콘 + 색 전면 교체 (앱만, 시트 재배포 불필요)

앱 아이콘을 길드 마스코트(주판을 든 고블린 회계사)로 바꾸고, **그 그림에서 색을 뽑아**
앱 전체를 다시 칠했습니다.

| 그림의 요소 | 앱에서 쓰는 곳 |
|---|---|
| 밤의 돌바닥 · 갑옷 | 배경 · 헤더 |
| 룬의 청록빛 | 브랜드 — 버튼 · 활성 탭 · 입력 포커스 |
| 금화 · 주판의 황동 | 돈을 가리키는 강조 — 합계 · 버전 · 헤더 테두리 |
| 양피지 | 밝은 화면의 바탕 (예전의 푸른 회색 대신) |

- **★ 금색을 버튼에 쓰지 않았습니다.** 분배전(주황)과 구분이 안 돼 "받을 돈"이 묻힙니다.
  청록은 분배전(주황)·분배완료(초록) 어느 쪽과도 섞이지 않습니다
- `--on-brand` 를 새로 뒀습니다. 어두운 화면에서 밝은 청록 버튼에 흰 글씨를 얹으면
  대비가 모자라 글씨가 흐릿합니다 — 그때는 글씨를 어둡게 뒤집습니다
- **활성 탭에 금색 밑줄**을 넣었습니다. 색만으로 구분하면 색각 이상이 있는 사람에게는
  표시가 없는 것과 같습니다
- 헤더에 마스코트를 넣어 홈 화면 아이콘과 **같은 얼굴**이 되게 했습니다.
  좁은 폰에서 제목을 밀어내지 않도록 그림은 줄바꿈 흐름 밖에 두고,
  한국어가 `길드정 / 산` 처럼 끊기지 않게 `word-break: keep-all` 을 걸었습니다
- 아이콘 4종 재생성(192·512·180·maskable). maskable 은 안드로이드가 원·사각형으로
  잘라내므로 **안쪽 80% 에 얼굴이 들어오도록** 따로 만들었습니다 — 같은 파일을 쓰면 귀가 잘립니다
- 용량: 512 아이콘 690KB → **168KB** (설치할 때마다 받는 파일입니다)

---

## v10.8.2 — 혈맹원 저장 버튼을 하나로 (앱만, 시트 재배포 불필요)

`[이름 저장]` 과 `[설정 저장]` 이 따로 있었습니다. 관리자에게는 **"이 사람을 고친다"**는
하나의 일인데 버튼이 둘이면 어느 쪽이 저장됐는지 알 수 없고, 한쪽만 누르고 창을 닫기도 쉽습니다.

```
아이디            [잡이K]
한자표기 (중문)    [卡尔K]
                  "잡이K (卡尔K)" 로 나옵니다
[따라오는 분배전 · 기존 게임표시명]
분배비중 (%)       [100%]
서버               [(지정 안 함)]
[취소]   [저장]                  ← 하나
```

- 아이디는 개명 API, 나머지는 설정 API 로 가지만 **한 번 눌러 둘 다** 처리합니다
- ★ **개명이 먼저**입니다. 설정을 먼저 저장하면 옛 이름 행에 쓴 뒤 그 행이 사라집니다
- 개명은 끝났는데 설정 저장이 실패하면 그렇게 알립니다
- 검사가 **저장 버튼이 정확히 1개인지** 확인합니다. 예전 검사는 함수 이름이 바뀌면
  조용히 통과하는 구멍이 있었는데(찾지 못하면 비교가 `-1 > -1`), 못 찾으면 실패하도록 고쳤습니다

---

## v10.8.1 — 한자표기 마무리 (앱만, 시트 재배포 불필요)

> **세 번째 자리는 앱에만 있습니다.** 시트를 건드리지 않는 화면 수정은 여기만 올립니다.
> `.gs` 는 v10.8 그대로 두시면 되고, 헤더의 `v10.8.1` 로 앱이 갱신됐는지 확인하실 수 있습니다.

- **[혈맹원 관리] 명단**에도 한자를 이름 옆에 붙였습니다. 아래 줄에 작게 두니
  아이디에 괄호로 넣은 사람(`SogeKing (狙击王)`)과 모양이 달라, 같은 명단인데
  "얘만 한자가 없네"로 보였습니다
- 규칙의 본체를 `mergeName()` 으로 내려 `nameParts`·[혈맹원 관리]가 **같은 한 벌**을 씁니다.
  규칙이 두 벌이 되면 화면마다 다르게 보이기 시작합니다
- **버전 경고를 앞 두 자리로만 비교**합니다. 앱만 10.8.1 로 올렸을 때
  멀쩡한 시트에 "옛 버전" 경고가 붙으면, 다시 붙여넣게 만드는 거짓 경고가 됩니다

---

## v10.8 — 보스 시간표(레이드) · 내용 공유 · 탭 순서

> `.gs` 를 붙여넣고 **[배포 관리] → 새 버전**으로 올린 뒤,
> 앱 [⚙️ 관리] → 관리 도구 → **[🗡️ 보스 시간표 기본값 채우기]** 를 한 번 실행하세요.

**🗡️ 레이드 탭 — 오늘 것만 보여줍니다**

시간표 전체(46종)를 한 화면에 띄우면 폰에서 읽을 수가 없어서, 결국 아무도 안 봅니다.
그래서 **접속한 그날 요일 것만** 먼저 나옵니다. 요일 칩을 누르면 다른 요일도 미리 볼 수 있습니다.

- 새 시트 **[레이드]** — 열은 `요일 · 시간 · 보스 · 비고 · 수정자 · 수정일`
- ⭐ **한 보스가 여러 요일에 나오면 요일마다 한 줄**입니다.
  한 칸에 `월,수,금` 처럼 몰아 넣으면 앱이 요일로 가려낼 수 없습니다
- 요일은 1(월)~7(일). 시트에는 `월`·`화` 로 적고 `_normDay` 가 숫자로 바꿉니다 (`월요일` 도 인식)
- 시간은 `HH:MM` 24시간. `_normTime` 이 Date 셀·`20시20분`·`20:20` 을 같은 값으로 만듭니다.
  **못 읽으면 빈 값입니다 — 지어내지 않습니다.** 요일·시간·보스 중 하나라도 비면 그 줄은 화면에 올리지 않습니다
- 요일 판정은 **폰의 시계**로 합니다. 서버에서 걸러 보내면 중국에서 접속했을 때
  자정 근처에 엉뚱한 요일이 나오고, 요일을 바꿔 볼 때마다 왕복이 생깁니다
- 추가·수정·삭제는 **관리자·마스터관리자 모두** 가능합니다.
  잘못 지워도 한 줄 다시 넣으면 끝이라 마스터 전용으로 두지 않았습니다

**📤 내용 공유 버튼**

길드 운영은 결국 카카오톡·디스코드에서 이뤄집니다. 스크린샷은 글자가 작아 읽히지 않고
검색도 안 되므로, **텍스트로** 내보냅니다. 폰에서는 공유 시트가 뜨고, 안 되면 클립보드로 물러섭니다.

| 붙은 곳 | 안 붙은 곳 |
|---|---|
| 잔액 · 아이템 · 연합 · 레이드 · 내 정보 | **게시판 · 관리** |

게시판 글은 그 자체가 이미 공유물이고, 관리 탭에는 PIN·도구처럼 밖으로 나가면 안 되는 것이 섞여 있습니다.
공유 시트를 그냥 닫은 것(`AbortError`)은 실패로 알리지 않습니다 — 실패로 띄우면 같은 걸 여러 번 보내게 됩니다.

**🀄 참여자 칩의 한자를 국문보다 크게**

글로벌 길드입니다. 중국 길드원에게는 **한자 쪽이 본명**이라, 그게 안 읽히면
아이템 등록 화면에서 자기 칸을 못 찾습니다.

- 한자 기본 크기 12px → **19px** (국문 14px보다 큽니다), 최소 12px
- 괄호 `( )` 를 뗐습니다 — 폭을 20% 넘게 먹는데, 두 줄로 나뉜 자리와 색만으로
  한자 표기인 것은 이미 드러납니다. 그만큼 글자가 커집니다
- 두 줄이 **같은 폭 예산**(`CHIP_NAME_PX = 64px`)을 나눠 쓰도록 `fitIn()` 을 넣었습니다.
  예전처럼 "몇 글자까지는 그대로"를 줄마다 손으로 맞추면, 한쪽 크기를 바꿀 때
  다른 쪽을 같이 고쳐야 하는데 그걸 잊으면 글자가 삐져나갑니다
- 아무리 길어도 **자르지 않습니다** — 크기를 줄이고, 하한(12px)에 닿으면 줄바꿈합니다.
  잘린 이름은 다른 사람으로 오인돼 엉뚱한 사람이 참여자로 체크됩니다

**🀄 멤버DB "한자표기"가 어느 화면에도 안 나오던 문제**

관리자가 [혈맹원 관리]에서 한자표기 칸에 넣은 값이 **잔액·아이템 어디에도 나오지 않았습니다.**
한자가 보이던 사람들은 전부 아이디 자체에 `SogeKing (狙击王)` 처럼 괄호를 달아둔 경우였습니다.

한자가 들어올 수 있는 자리가 둘인데 화면은 하나만 보고 있었습니다.

| | 자리 | 예전 |
|---|---|---|
| ① | 멤버DB **G열 한자표기** (관리자가 직접 입력) | **무시됨** |
| ② | 아이디 속 괄호 `SogeKing (狙击王)` | 표시됨 |

- `nameParts()` 하나로 모았습니다 — **①이 우선**, 없으면 ②로 물러섭니다.
  관리자가 명시적으로 지정한 값이 이름에서 유추한 것보다 확실합니다
- 잔액 · 아이템 칩 · 등록 확인창 · 내 정보 · 지급 창이 전부 같은 함수를 씁니다.
  한 곳만 고치면 다른 화면에서 또 사라지므로, 검사가 다섯 화면을 전부 확인합니다
- ①과 ②가 같은 값이면 한 번만 씁니다 (`잠단(斬斷)` + G열 `斬斷` → `잠단 (斬斷)`)
- **없으면 만들지 않습니다** (규칙 7). 명단에 없는 사람도 터지지 않습니다
- 잔액 목록의 한자를 12px → **14px** 로 키웠습니다

**입력칸 배치 — 아이디 바로 아래에 중문**

예전에는 아이디(맨 위)와 한자표기(맨 아래) 사이에 분배비중·서버가 끼어 있어서
한자 칸이 있다는 것 자체를 모르고 지나가기 쉬웠습니다.

```
아이디 (한글·영문)   [잡이K]
한자표기 (중문)      [卡尔K]   ← 바로 아래로
                    "잡이K (卡尔K)" 로 나옵니다   ← 실제 표시될 모양 미리보기
[취소]  [이름 저장]                                ← 둘을 한 번에 저장
```

- 버튼이 하나입니다. 칸만 붙여 놓고 저장 버튼을 둘로 두면 어느 쪽이 저장됐는지 알 수 없습니다
- ★ **개명을 먼저** 하고 한자표기는 **바뀐 이름**으로 저장합니다.
  반대로 하면 옛 이름 행에 저장한 뒤 그 행이 사라집니다
- 개명은 끝났는데 한자 저장이 실패하면 그렇게 알립니다 — 실패로만 알리면 개명을 다시 시도하게 됩니다

**하단 탭 순서**

```
이전:  잔액  아이템  게시판  연합    내정보  관리
이후:  잔액  아이템  연합    레이드  내정보  게시판  관리
```

**검증**

- `npm run verify:gs` 51건 — 레이드 2건 · 한자표기 1건 신규.
  한자 해석 5케이스를 실제로 실행하고, 다섯 화면이 `nameParts` 를 쓰는지 · `splitName` 직접 사용이
  0곳인지 · 입력칸이 인접한지 · 개명이 한자 저장보다 먼저인지 확인합니다.
  (검사가 실제로 잡아내는지, 버그를 되돌려 넣어 실패하는 것까지 확인했습니다)
- `npm run e2e` 96건 — 레이드 4건 · 한자표기 2건 신규.
  괄호 없는 이름(`TC무식` + G열 `车武植`)이 잔액·칩·내 정보에 나오는지, 중복 표기가 없는지,
  한자 없는 사람에게 만들어 붙이지 않는지, 입력칸 순서, 저장이 잔액까지 이어지는지

---

## v10.7 — 한자 인식률

> `.gs` 를 붙여넣고 **[배포 관리] → 새 버전**으로 올려야 합니다.

**한자가 계속 빠지던 원인 — 셋 다 제 쪽 문제였습니다**

1. **한자 패스를 아예 시도하지 않고 있었습니다.**
   `_ocrImageMulti` 가 "글자 수 40 을 넘으면 충분하다"며 멈췄는데,
   한글 패스만으로도 40 자는 쉽게 넘습니다. 결과적으로 **한글로만 읽고 끝났습니다.**
   이제 `ko`·`zh-CN` 두 패스를 **항상** 돌립니다
2. **좋은 결과를 고르는 기준이 틀렸습니다.** 글자 수로 비교하면
   한자 줄을 통째로 놓친 결과가 **더 좋아 보입니다** (한글만으로 수를 채우므로).
   `_ocrScore` 가 한자에 3배 가중치를 줍니다
3. **사진 보정이 한자를 망가뜨리고 있었습니다.** 1600px 축소 + 대비 160% 는
   한글 명단 기준으로 정한 값입니다. `鮮`·`籠` 처럼 획이 빽빽한 글자는
   조금만 줄여도 뭉개지고, 대비를 세게 올리면 **획 사이가 메워져 통글자**가 됩니다

**사진 보정을 용도별로 나눴습니다**

| | 최대 크기 | 대비 | 화질 |
|---|---|---|---|
| 이름을 **읽을** 때 (아이템·명단) | 2600px | 118% | 0.92 |
| 인원수만 **셀** 때 (연합) | 1600px | 160% | 0.82 |

- 작게 찍힌 화면은 **오히려 키웁니다** (1500px 미만이면 최대 2배) — OCR 은 글자가 너무 작으면 포기합니다
- 해상도를 올린 만큼 용량이 커지므로, 상한을 넘으면 **화질 → 크기 순으로 스스로 줄입니다.**
  안 그러면 "왜 안 되는지 모를 실패"가 됩니다
- 안내에 좋은 스크린샷 찍는 법을 넣었습니다 (명단만 크게 잘라서 · 나눠서 두세 번)

**검사 1개 추가 (verify 48건)**
- `OCR 은 한자를 놓친 결과를 더 좋게 보지 않는다` — `_ocrScore` 를 **실제로 실행해서**
  한자를 읽은 짧은 결과가 한글만 긴 결과보다 높게 나오는지 확인하고,
  "40자 넘으면 그만" 이 되살아나지 않는지, 두 패스를 항상 도는지
- `사진 인식` 검사에 **읽기용 축소 하한(2000px)·대비 상한(130%)** 을 넣었습니다.
  누가 다시 1600px/160% 로 되돌리면 실패합니다

---

## v10.6 — 개명 대상 자유 선택 + 사진 인식 개선 + 마스터 PIN 진단

> 아래 "마스터 PIN" 항목은 **앱만 고친 것**이라 `.gs` 를 다시 붙여넣을 필요가 없습니다.

**마스터관리자가 관리자의 상위 등급이 되었습니다**
- 나누는 기준은 하나입니다 — **되돌릴 수 있는가.**

| 마스터 전용이 된 것 | 왜 |
|---|---|
| 시즌 종료 · 데이터 이관 · 최초 설치 · 공장 초기화 | 관리자가 실수로 눌러도 복구할 방법이 없습니다 |
| 아이템 정정 · 삭제 | 이미 끝난 분배를 되돌리는 작업입니다 |
| 지급 취소 | 위와 같은 성격입니다 |

- **관리자가 할 수 있는 것은 마스터도 전부 할 수 있습니다.** 반대는 성립하지 않습니다
- **조회는 관리자에게 열어뒀습니다** — 정정 미리보기, 지급기록 조회.
  무엇이 잘못됐는지는 알아야 마스터에게 정확히 알릴 수 있습니다
- 일상 도구(참여횟수 재계산·시트 정돈·디스코드·시즌 서버·지난 시즌 가져오기)는
  **관리자 그대로**입니다. 권한을 과하게 조이면 업무가 막힙니다
- **관리자 화면에는 아예 보이지 않습니다.** 잠긴 버튼으로 남겨두면 "왜 안 되냐"를
  묻게 되고, 되돌릴 수 없는 작업이 목록에 계속 보이는 것 자체가 실수를 부릅니다:
  - 도구 목록에서 **걸러냅니다** (비활성이 아니라 미표시)
  - `지급 취소` 구역 통째로 미표시
  - 아이템 탭의 `정정·삭제` 카드 통째로 미표시
- 도구 등급 판정은 **시트가** 합니다. 라우트는 앱이 보낸 값이 아니라 시트에서 받은
  목록으로 판정하고, **목록을 못 받아오면 막는 쪽**으로 갑니다
- E2E 가 관리자 화면을 실제로 열어 그 이름들이 **하나도 없는지** 확인하고,
  마스터로 바꾸면 **전부 나타나는지**까지 봅니다

**앱 이름을 두 줄로 쓸 수 있습니다**
- 이름이 길면 헤더에서 저절로 줄이 바뀌는데, **어디서 끊길지는 화면 폭이 정합니다**
  (`리니지W길드매` / `니저` 처럼 엉뚱한 데서 끊깁니다). 이제 앱 이름 칸에서
  **엔터로 직접 끊을 자리를 정할 수 있습니다** (24자 · 2줄까지)
- 헤더 높이를 **고정에서 가변으로** 바꿨습니다. 고정이면 두 번째 줄이 아래 내용을 덮습니다
- 제목이 길어도 **오른쪽 칩(새로고침·시즌)은 찌그러지지 않습니다** — 눌러야 하는 것이므로
  제목 쪽이 줄어들고 칩은 제 크기를 지킵니다
- 줄 수 제한은 **앱·라우트·시트 세 곳**에 걸어뒀습니다

**마스터 PIN 이 "눈 버튼을 눌러야만" 되던 문제**
- 눈 버튼을 누르면(=`type="text"`) 되고, 안 누르면(=`type="password"`) 안 되는 증상은
  **비밀번호 관리자의 자동 채움** 때문입니다. 비밀번호 칸으로 인식되는 동안에는
  저장된 **관리자 PIN 이 덮어써지고**, `text` 로 바뀌는 순간 자동 채움이 손을 뗍니다
- 고친 것:
  - `autoComplete` 을 `current-password` → **`new-password`** 로 바꾸고
    (`data-lpignore`·`data-1p-ignore` 도 함께) 자동 채움을 막습니다
  - 로그인할 때 **화면에 실제로 들어 있는 값**을 보냅니다. 눈에 보이는 것이 곧 보내는 값입니다

**마스터관리자 PIN 이 안 먹던 문제 — 진짜 원인은 키보드였습니다**
- 배포된 `/api/health` 를 확인해보니 설정은 완전히 정상이었습니다
  (`set: true, sameAsAdmin: false, hadWhitespace: false, usable: true`).
- 원인은 **PIN 입력칸이 `inputMode="numeric"`** 이었던 것입니다. 폰에서 이 속성은
  **숫자 키패드만** 띄웁니다. 관리자 PIN 은 숫자라 문제가 없었지만, 문자가 섞인
  마스터 PIN 은 **아무리 정확히 알아도 입력할 방법이 자체가 없었습니다.**
- 함께 고친 것:
  - **자동 대문자·자동 수정을 껐습니다.** iOS 는 첫 글자를 대문자로 바꾸는데,
    PIN 은 대소문자를 가리므로 그것만으로도 로그인이 실패합니다
  - **👁 보기 전환 버튼**을 넣었습니다. 무엇을 입력했는지 볼 수 없으면
    오타인지 PIN 이 틀린 건지 구분할 수가 없습니다

**설정 쪽 원인도 함께 없앴습니다**
- 기능 자체는 v10.0 부터 구현돼 있고 판정 순서도 맞았습니다 (마스터 → 시트 PIN → 환경변수).
  문제는 **값**이었습니다. 원인이 될 수 있는 건 둘뿐인데, 화면에는
  "PIN이 올바르지 않습니다"만 떠서 스스로 알아낼 방법이 없었습니다:
  1. **Vercel 대시보드에 붙여넣을 때 딸려 들어간 공백·줄바꿈** —
     그러면 아무리 정확히 입력해도 영원히 틀린 값이 됩니다
  2. **`MASTER_PIN` 이 `ADMIN_PIN` 과 같은 값** — 같으면 마스터로 인정하지 않습니다
     (등급을 나눈 의미가 없어지므로). 이때 조용히 관리자로만 로그인됩니다
- 고친 것:
  - **환경변수와 입력 PIN 양쪽의 앞뒤 공백을 털어냅니다.** PIN 앞뒤 공백은 어떤 경우에도
    의도된 값이 아닙니다. 폰 키보드·비밀번호 관리자도 공백을 자주 붙입니다
  - **`/api/health` 가 스스로 진단합니다** — 값은 절대 내보내지 않고 원인만:
    ```json
    "master": { "set": true, "sameAsAdmin": true, "hadWhitespace": false, "usable": false,
                "msg": "⚠️ MASTER_PIN 이 ADMIN_PIN 과 같은 값입니다..." }
    ```
  - 로그인 결과에 코드를 붙여 화면 언어로 나오게 했습니다 (`auth.master`·`auth.admin`·`auth.badPin`)

**검사 1개 + E2E 2개 추가 (verify 45건 · E2E 81건)**
- `마스터 PIN: 공백이 붙어도 통하고, 관리자 PIN 과 같으면 거부한다` —
  양쪽 공백 제거, 동일값 거부, **마스터 판정이 관리자보다 먼저인지**,
  health 가 값을 노출하지 않는지. (가드를 지워보고 검사가 실제로 실패하는 것까지 확인했습니다)
- E2E: 앞뒤 공백이 붙은 마스터 PIN 으로 로그인되는지, health 응답에 PIN 값이 안 들어가는지
- 결과 코드 검사 범위를 **서버 라우트까지** 넓혔습니다 (예전엔 `.gs` 만 봤습니다)


> `.gs` 를 붙여넣고 **[배포 관리] → 새 버전**으로 올려야 동작합니다.

**개명 대상을 전체 명단에서 고릅니다**
- 예전에는 "비슷해 보이는 사람"이 있을 때만 [개명] 버튼이 눌렸습니다. 그런데 실제로는
  `테리` 처럼 **전혀 다른 이름으로 갈아타는 경우가 대부분**이라, 후보가 0명이면
  개명 자체를 못 하고 → 잔액·참여횟수·시즌기록이 **0부터 다시 시작**했습니다
- 이제 판정 결과에 **전체 명단**을 함께 실어 보내고, 드롭다운에서 아무나 고를 수 있습니다.
  비슷한 사람은 `(비슷함)` 표시와 함께 맨 위로 올립니다
- 버튼 이름을 `추가` → **`신규`** 로 바꿨습니다 (신규 / 개명 / 건너뜀)
- **한 아이디를 두 사람이 물려받을 수 없습니다.** 앱에서는 이미 선택된 아이디가
  `— OOO 가 선택함` 으로 비활성화되고, **서버도 같은 검사를 합니다** —
  앱은 고칠 수 있으므로 최종 판정은 서버가 합니다.
  그냥 두면 먼저 처리된 쪽만 잔액을 가져가고 뒤쪽은 조용히 실패합니다
- 명단에 없는 아이디를 지정하면 거부합니다. 두 검사 모두 **실제 개명이 일어나기 전에** 끝납니다
- 개명 대상을 아직 안 고른 줄은 테두리가 주황색이 되고 실행 버튼이 잠깁니다

**사진에서 글자를 못 읽던 문제 — 원인이 셋이었습니다**

1. **언어 힌트가 한국어 하나뿐이었습니다.** 우리 명단은 한글·한자·영문이 한 화면에
   섞여 있어서, `ko` 로만 읽으면 `卡尔K` 같은 줄이 통째로 빠집니다.
   이제 `ko` → `zh-CN` → `en` 순으로 읽어보고 **가장 많이 건진 결과**를 씁니다.
   충분히 읽혔으면 나머지는 시도하지 않습니다 (Drive 호출 절약)
2. **명단 일괄 추가만 사진 보정을 안 하고 있었습니다.** 아이템 탭에는 축소(1600px)와
   명암비 보정(160%)이 있는데 일괄 추가는 **원본 수 MB 를 그대로** 보내고 있었습니다.
   폰 사진은 그대로 보내면 OCR 이 오히려 더 못 읽습니다.
   보정을 `lib/client.ts` 의 `prepPhoto` 하나로 모으고 **세 화면이 같이 씁니다**
3. **실패 이유를 감추고 있었습니다.** "글자를 읽지 못했습니다"만 보여주니
   사진 탓인 줄 알고 계속 다시 찍게 됩니다. 실제 원인은 대부분
   **Apps Script 에 Drive API 서비스가 없는 것**이고, 그건 한 번이면 해결됩니다.
   이제 그 안내를 그대로 보여줍니다:
   > `[서비스] → [+] → "Drive API" 를 추가한 뒤 저장하면 됩니다.`
- 읽어낸 원문을 화면에서 볼 수 있고(`인식된 텍스트 보기`), **[읽은 글자를 입력칸에 넣기]**
  로 옮겨서 손으로 고친 뒤 다시 판정할 수 있습니다

**검사 2개 + E2E 2개 추가 (verify 44건 · E2E 79건)**
- `개명 대상은 전체 명단에서 고르고, 한 명을 두 번 물려받을 수 없다` —
  판정이 전체 명단을 싣는지, 서버가 중복·미존재를 거부하는지,
  **그 검사가 `_renameCore` 실행보다 앞에 있는지**, 앱이 후보 0명이라고 막지 않는지
- `사진 인식: 여러 언어로 읽고, 실패 이유를 감추지 않는다` —
  3개 언어를 다 시도하는지, **일부 언어만 실패해도 전체를 실패로 만들지 않는지**,
  Drive API 안내가 있는지, 세 화면이 모두 `prepPhoto` 를 쓰고 옛 경로가 안 남았는지
- E2E: 전혀 안 닮은 이름으로 개명해도 잔액이 승계되는지,
  중복 지정이 거부되고 **거부되면 앞쪽도 반영되지 않는지**

---

## v10.5 — 정원 50명 → 100명

> `.gs` 를 붙여넣고 **[배포 관리] → 새 버전**으로 올리면 끝입니다. 따로 하실 일은 없습니다.

- 최대 혈맹원 수를 **50명 → 100명**으로 올렸습니다
- **상수만 올리면 안 됩니다.** 50명 기준으로 만들어진 지금 시트에 100행을 읽으면
  "범위가 시트를 벗어난다"며 터져서, 붙여넣는 순간 **조회조차 안 됩니다.**
  그래서 두 가지를 함께 넣었습니다:
  - `_ensureCapacity()` — 멤버DB · 참여자현황 · 잔액현황의 행을 자동으로 늘립니다.
    `doPost` 와 `onOpen` 양쪽에서 부릅니다 (앱으로 들어오든 PC로 열든 반드시 지나가는 길목).
    이미 충분하면 아무 일도 하지 않습니다 — 행 수를 읽어보는 것뿐이라 비용이 없습니다
  - `_memberBlock()` — 멤버 블록은 **시트에 실제로 있는 만큼만** 읽습니다.
    행 늘리기가 실패해도(시트 보호 등) 읽기가 통째로 터지지 않습니다
- 정원을 쓰는 곳은 전부 `MAX_MEMBERS` 상수를 참조하므로 다른 수정은 없습니다
  (사용안내 문구, 추가 시 상한 검사, 일괄 추가의 남은 자리 계산 등)

**검사 1개 추가 (verify 42건)**
- `정원을 올려도 옛 시트에서 터지지 않는다` —
  ① 멤버 블록을 고정 크기로 읽는 곳이 **하나라도 남아 있으면 실패**
  ② `_ensureCapacity` 가 `doPost`·`onOpen` 양쪽에서 불리는지
  ③ `_memberBlock` 을 실제로 실행해 1행·2행·51행·101행·1000행짜리 가짜 시트에서
     범위를 넘지 않고 정확한 줄 수를 읽는지
  ④ 모의 시트의 정원이 `.gs` 와 같은지 (다르면 E2E 가 의미를 잃는다)

---

## v10.4 — 명단 일괄 추가 (사진·텍스트) + 오류 검증

> `.gs` 를 붙여넣고 **[배포 관리] → 새 버전**으로 올려야 동작합니다.

**한 번에 명단을 넣습니다 — 다만 넣기 전에 반드시 걸러냅니다**
- `[⚙️ 관리] → 아이디 관리 → 📋 명단 일괄 추가`
- 게임 명단 **스크린샷**을 넣거나, 텍스트를 **통째로 붙여넣습니다**
  (쉼표·줄바꿈 아무거나, `1.` `01)` 같은 앞 번호는 알아서 떼어냅니다)
- 이 기능의 핵심은 "빨리 넣는 것"이 아니라 **넣기 전에 걸러내는 것**입니다.
  40명을 잘못 넣으면 되돌리기가 아주 번거롭습니다. 그래서 두 단계입니다:
  ```
  ① 판정  아무것도 쓰지 않고 한 줄씩 상태만 매긴다
  ② 실행  관리자가 표를 보고 확정한 것만 반영한다
  ```
- 판정은 **서버(시트)가** 합니다. 앱에서만 검사하면 앱을 고쳐서 우회할 수 있습니다

**판정 결과 5가지**

| 표시 | 뜻 | 기본 동작 |
|---|---|---|
| 신규 | 명단에 없는 새 이름 | 추가 |
| 이미 있음 | 이미 멤버DB에 있음 | 건너뜀 |
| 입력 중복 | 붙여넣은 안에서 두 번 나옴 | 건너뜀 |
| 개명 후보 | 기존 멤버와 표기가 비슷함 | **건너뜀 — 사람이 고름** |
| 확인 필요 | 이름으로 볼 수 없음 (숫자·한 글자 등) | 건너뜀 |

**개명으로 지정하면 기록이 승계됩니다 — 이게 "누적 관리"의 핵심입니다**
- 개명은 추가가 아니라 `_renameCore` 를 탑니다. 분배전·분배완료·참여횟수·
  지난 시즌 기록이 **새 이름으로 그대로 넘어갑니다.** 새로 추가하면 0부터 시작합니다
- **개명 후보를 시스템이 확정하지 않습니다.** 잘못 이으면 두 사람 잔액이 합쳐집니다.
  제안만 띄우고 관리자가 "누구였는지"를 직접 고릅니다
- 개명을 추가보다 **먼저** 실행합니다 — 옛 이름과 새 이름이 빈 칸을 두고 다투지 않도록

**지킨 안전장치**
- **번호를 떼고 나니 빈 줄이 되는 입력(`42` 등)을 버리지 않습니다.** 조용히 지우면
  OCR 이 이름을 숫자로 잘못 읽었을 때 그 사람이 소리 없이 누락되고, 관리자는
  누가 빠졌는지 영영 모릅니다. '확인 필요'로 남겨 보여줍니다
- 실행 전 **재확인**을 받습니다 (위험도 2). 서버 라우트가 `confirm` 을 임의로 채우지 않습니다
- **대상별 개별 try/catch** — Apps Script 에는 트랜잭션이 없습니다. 무엇이 되고
  무엇이 안 됐는지 이름까지 정확히 돌려줍니다 (규칙 6)
- 정원(50명) 초과는 실행 전에 막습니다
- 서버 번호는 **이번에 추가·개명한 사람에게만** 반영합니다. 기존 멤버는 손대지 않습니다

**검사 1개 + E2E 4개 추가 (verify 41건 · E2E 77건)**
- `명단 일괄 추가: 판정은 쓰지 않고, 실행은 재확인을 거친다` —
  판정 함수에 쓰기 호출(`setValue`·`deleteRow` 등)이 하나라도 있으면 실패,
  재확인 게이트·개별 try/catch·정원 검사·개명이 `_renameCore` 를 타는지·
  개명이 추가보다 먼저인지·라우트가 `confirm` 을 그대로 넘기는지·
  앱이 개명 후보를 자동 확정하지 않는지
- E2E: 상태 5종 판정, 판정 단계가 명단을 안 바꾸는지, 확인 없이는 반영 안 되는지,
  **개명이 잔액·참여횟수를 승계하고 신규는 0부터 시작하는지**,
  서버가 손댄 사람에게만 붙는지, 잘못된 요청이 시트까지 안 가는지

---

## v10.3 — 연합 2단계 분리 · 이름 두 줄 표기 · 잔액 서버 표시

> `.gs` 를 붙여넣고 **[배포 관리] → 새 버전**으로 올려야 연합 2단계가 동작합니다.

**연합 정산이 혈맹 아이템과 같은 순서가 되었습니다**
- 예전에는 서버·아이템명·금액·비중·인원을 **한 번에** 받았습니다. 그런데 레이드 직후엔
  아직 안 팔려서 금액을 모르는 것이 정상이라, 등록 자체를 미루게 되고 그 사이에
  인증샷을 잃어버립니다
- 이제 두 단계입니다:
  ```
  ① 등록  서버 · 아이템명 · 인증샷(인원수)   → ⏳미분배
  ② 정산  금액 · 비중(%)                    → ✅분배완료, 서버별 누적
  ```
- `[연합]` 시트에 **상태 열(J)** 이 생겼습니다. v10.2 이하 시트는 앱이 처음 열 때
  헤더만 자동으로 채웁니다(`_ensureAllianceHeaders`) — 기존 행은 그대로 쓰이고,
  금액이 있으면 ✅분배완료로 읽습니다
- **아직 금액이 안 정해진 건은 서버별 누적에 넣지 않습니다.** 넣으면 0원짜리가 건수만 부풀립니다
- **이미 정산된 건을 다시 정산하는 것은 거부합니다.** 두 번 누적되면 서버 총액이 틀어집니다
- API: `api_addAlliance` 에서 금액·비중이 빠지고, `api_creditAlliance` 가 새로 생겼습니다

**참여자 선택 화면의 이름이 두 줄로 나옵니다**
- `잡이K (卡尔K)` 를 국문 위 · 한문 아래 두 줄로 나눠 보여줍니다
- 글자 **수**가 아니라 **폭**을 재서 글씨 크기를 줄입니다. 한글·한자는 라틴 문자보다
  두 배 가까이 넓어서, 개수로만 세면 `PlusS`(5자)와 `선륙소농포`(5자)를 같게 보는데
  실제로는 뒤쪽만 잘립니다
- 잘린 이름은 다른 사람으로 오인돼 **엉뚱한 사람이 참여자로 체크되므로** 잘리게 두지 않습니다.
  E2E 가 실제로 그려진 폭(`scrollWidth`)이 칩 안에 들어가는지 확인합니다
- `(미등록)` 은 한자 표기가 아니라 상태 표시라 둘째 줄로 올리지 않습니다

**잔액 탭에 서버 번호가 붙습니다**
- 멤버DB F열의 서버를 이름 앞에 배지로 보여줍니다 (`01` `04` …)
- 서버가 지정되지 않은 멤버에는 붙이지 않습니다 — 빈 배지는 오해를 만듭니다
- 이름 비교는 `normName` 을 거칩니다 (규칙 4)

**검사 3개 + E2E 4개 추가 (verify 40건 · E2E 73건)**
- `연합은 등록과 정산이 분리되어 있다` — 등록 단계가 금액을 안 받는지, 정산이 락 안에서
  돌고 중복 정산을 거부하는지, 미정산 건이 누적에서 빠지는지, 옛 시트 헤더 보정이 있는지
- `이름은 두 줄로 나뉘고 글자 수에 맞춰 줄어든다` — 실제 `splitName`·`fitFont` 를 실행해
  괄호 분리·`(미등록)` 예외·**폭 기준 축소**·최소 크기 하한을 확인
- `잔액 목록에 서버 번호가 보인다`
- E2E: 등록→정산 2단계 왕복, 미정산 건이 누적에 안 섞이는지, 재정산 거부 후 총액이 그대로인지,
  칩 안에서 이름이 잘리지 않는지, 긴 한글 이름이 실제로 작게 그려지는지, 서버 배지

---

## v10.2 — 반영 속도: 왕복 한 번 제거

> `.gs` 를 붙여넣고 **[배포 관리] → 새 버전**으로 올려야 효과가 납니다.
> 안 올려도 앱은 그대로 동작합니다 — 예전처럼 조회를 한 번 더 할 뿐입니다.

**쓰기 응답에 최신 상태를 같이 실어 보낸다**
- 앱은 등록·분배·지급 뒤에 화면 숫자를 맞추려고 상태를 다시 읽어야 하는데,
  예전에는 그것이 **두 번째 요청**이었다:
  ```
  [폰] ── 쓰기 ──▶ [Vercel] ──▶ [시트]      (왕복 1)
  [폰] ── 조회 ──▶ [Vercel] ──▶ [시트]      (왕복 2)
  ```
  왕복마다 폰↔서버 네트워크 지연 + Apps Script 실행 준비 비용이 다시 든다.
- 이제 시트가 쓰기와 **같은 실행 안에서** 상태를 읽어 응답에 붙인다 (`_withState`).
  이미 열려 있는 스프레드시트를 그대로 읽으므로 시트를 다시 여는 비용도 들지 않는다
- 요청이 `withState: true` 를 보낸 **쓰기 액션**에만 붙인다.
  무조건 붙이면 상태가 필요 없는 호출까지 매번 전체 시트를 읽는다
- Vercel 은 그 값을 그대로 상태 캐시에 넣는다 (`syncStateCache`) —
  **다른 사람의 다음 조회도** 구글시트를 거치지 않고 바로 받는다
- 앱은 응답에 상태가 있으면 그걸 바로 화면에 반영하고, 없으면 (옛 버전 시트이거나
  시트 쪽 읽기가 실패한 경우) 예전처럼 `?fresh=1` 조회로 물러선다

**상태 읽기가 실패해도 쓰기는 성공 그대로 보고한다**
- 쓰기는 이미 끝났다. 읽기가 실패했다고 실패로 보고하면 사용자가 같은 분배를
  다시 실행한다 — **이중 분배 위험**. `_withState` 는 읽기 예외를 삼키고
  쓰기 결과를 그대로 돌려준다. 검사가 이 동작을 직접 확인한다

**검사 2개 + E2E 2개 추가 (verify 37건 · E2E 69건)**
- `쓰기 응답이 최신 상태를 같이 실어 보낸다` — `_withState` 를 실제로 실행해
  ① 붙는지 ② `withState` 없으면 안 붙는지(시트를 읽지도 않는지)
  ③ 실패한 쓰기·조회 액션에는 안 붙는지 ④ **읽기가 예외를 던져도 성공이 유지되는지**
- `앱이 실어 온 상태를 실제로 쓴다` — 시트 → `callGas` → 라우트 → 캐시 → 화면
  전 구간이 연결됐는지. 상태를 반영하는 쓰기 라우트가 `withState` 를 빠뜨리면 실패
- E2E: 등록 응답 하나로 화면을 다시 그릴 수 있는지, 그 값이 캐시에도 들어가
  다른 사람이 시트를 거치지 않고 받는지, 조회 응답에는 중복해서 붙지 않는지

---

## v10.1 — 결과 메시지까지 3개국어 (한국어 · 中文 · English)

> 아래 "반영 속도" 항목은 **앱만 고친 것**이라 `.gs` 를 다시 붙여넣을 필요가 없습니다.

**입력한 값이 화면에 늦게 나오던 문제**
- 원인 셋이 겹쳐 있었다:
  1. 캐시는 서버 인스턴스 **메모리**에 있다. 쓰기를 처리한 인스턴스에서 캐시를 버려도,
     그다음 조회가 아직 낡은 값을 든 **다른 인스턴스**로 가면 사용자는 옛 숫자를 본다
  2. 캐시 유효시간이 길었다 (상태 8초 · 게시판 15초 · 연합 10초)
  3. 다른 사람이 등록·분배한 것은 내가 앱을 다시 열기 전까지 전혀 따라오지 않았다
- 고친 방식:
  - **`?fresh=1`** — 등록·분배·지급·글쓰기 **직후의 조회만** 캐시를 건너뛴다 (`lib/fresh.ts`).
    어느 인스턴스에 떨어지든 그 인스턴스의 캐시를 먼저 버리고 시트에서 다시 읽으므로,
    내가 방금 넣은 값은 항상 즉시 보인다.
    평소 조회까지 fresh 로 부르면 캐시가 없는 것과 같아져 Apps Script 할당량을 그대로 태우므로
    **쓰기 직후에만** 쓴다 — 검사가 이걸 강제한다
  - 캐시 유효시간 단축: 상태 8초 → **4초**, 게시판 15초 → **8초**, 연합 10초 → **8초**
  - **25초 자동 갱신** — 앱을 켜두면 다른 사람이 올린 것도 저절로 따라온다.
    화면이 꺼지거나 다른 앱으로 넘어가면 멈춘다 (할당량 낭비 방지).
    서버 캐시가 뒤에서 요청을 흡수하므로, 보는 사람이 몇 명이든 구글시트가 실제로
    열리는 횟수는 늘지 않는다

**새로고침 버튼**
- 헤더 오른쪽에 있던 작은 `↻` 를 **누르기 쉬운 버튼**으로 바꾸고,
  옆에 `방금` · `3분 전` 처럼 **마지막으로 받아온 시각**을 함께 띄운다 —
  지금 눌러야 하는지 사용자가 스스로 판단할 수 있게 하는 것이 목적이다
- 누르는 동안 아이콘이 돌고, 이 버튼은 항상 캐시를 건너뛴다
- `prefers-reduced-motion` 을 켠 기기에서는 회전하지 않는다

**검사 5개 추가 (verify 35건 · E2E 67건)**
- `쓰기 라우트는 쓴 뒤에 캐시를 버린다` — 관리자·마스터 쓰기 라우트 18개 전수.
  사진 분석처럼 화면 데이터를 바꾸지 않는 것은 **액션 이름으로** 면제한다
  (경로로 면제하면 나중에 같은 폴더에 진짜 쓰기가 들어와도 통과해버린다)
- `쓰기 직후 조회는 캐시를 건너뛴다` — 조회 3종의 `?fresh=1` 지원 + TTL 상한(10초) +
  앱의 쓰기 완료 콜백이 전부 fresh 인지 + 주기 갱신은 fresh 가 **아닌지**
- `새로고침 버튼이 화면에 있다`
- E2E: 캐시가 실제로 동작하는지와 `?fresh=1` 이 그걸 건너뛰는지를 구분해서 확인,
  등록 직후 TTL 을 기다리지 않고 목록에 나타나는지, 헤더 버튼을 눌러 최신 값을 받아오는지


**앱 전체가 세 언어로 나옵니다**
- 화면 문구 353개 항목 × 3개 언어. `[⚙️ 관리]` 탭 맨 위에서 전환합니다
- 언어는 `localStorage` + 쿠키(`gm_lang`)에 남고, Vercel 라우트가 그 쿠키를 읽어
  Apps Script 호출에 실어 보냅니다 (라우트마다 따로 넘기면 빠뜨리는 곳이 생깁니다)

**시트가 돌려주는 결과 메시지도 화면 언어로 나옵니다 — 방식이 중요합니다**
- 시트는 문장을 세 벌로 만들지 **않습니다.** "무슨 일이 있었는지"를
  **코드 + 값**으로만 내려주고(`_rc`), 문장은 앱이 자기 사전으로 조립합니다
  ```js
  { ok: true, code: 'dist.ok',
    vars: { item: '광폭성검', amount: 52000, fundTotal: 12500, n: 3, per: 15000 },
    msg:  '✅ "광폭성검" 52,000다이아 분배 완료 …' }   // msg 는 한국어 폴백
  ```
- 처음에는 시트에서 어구를 치환하는 방식(`MSG_DICT`)으로 만들었다가 폐기했습니다.
  `⚠️ 아직 받지 않 待发放 잔액이 9,299钻石 남아 있습니다` 처럼 **문장이 반쯤만
  번역되기 때문**입니다. 코드+값 방식은 문장 전체를 각 언어로 따로 쓰므로 이런 일이 없습니다
- 사전에 없는 코드는 시트가 함께 보낸 한국어 `msg` 로 자동 폴백합니다 —
  드문 오류를 반쪽만 번역해 깨뜨리는 것보다 낫습니다
- 사람 이름·아이템명·계정명은 `vars` 로 그대로 흐르며 절대 번역하지 않습니다 (규칙 7)
- `danger:3` 확인 문구(`시즌종료`·`전부삭제` 등)는 어떤 언어에서도 그대로입니다.
  번역되면 서버의 문구 비교가 영원히 실패합니다

**검사 3개 추가 (총 32건)**
- `.gs 결과 코드가 앱 사전에 전부 있다` — 시트가 쓰는 71개 코드 ↔ 사전 71개 양방향 대조.
  한쪽에만 있으면 실패합니다
- `결과 문장의 자리표시자를 시트가 전부 채운다` — `s.dist.ok` 가 `{fundTotal}` 을 쓰는데
  시트가 그 값을 안 보내면 화면에 `{fundTotal}` 이 그대로 찍힙니다. 세 언어 모두 검사
- `확인 문구는 어느 언어에서도 번역되지 않는다`
- E2E 2건 추가 (총 64건): 영문 화면 전수 검사, 서버 결과 메시지 언어 검사
  (중문·영문에서 실제로 분배를 실행해 토스트에 한글이 남지 않는지 확인)

---

## v10.0 — 분배비중 · 연합 · 게시판 · 마스터관리자

**지난 시즌 보기 (실사용 반영)**
- `[📚 지난 시즌 기록만 가져오기]` 도구 신설. 옛 파일의 `[시즌N]` 시트만 복사하고
  멤버·잔액·아이템·작업기록은 **전혀 건드리지 않는다**.
  기존 `[📥 기존 파일에서 가져오기]` 는 멤버DB 이름을 통째로 덮어써서, 새 파일에서
  이미 개명·추가를 한 뒤에는 쓸 수 없다 (실제로 그 상황이 생겼다)
- 시즌 상세 화면이 **캐릭터명 + 정산 다이아** 목록으로 바뀌었다 (큰 금액순).
  아이템·지급 이력 표는 `[자세히 보기]` 를 눌렀을 때만 펼친다 —
  실제로 확인하고 싶은 건 "지난 시즌에 누가 얼마 받았나" 하나였기 때문
- 보관된 시즌이 하나도 없을 때, 빈 화면 대신 가져오기 방법을 안내한다

> ⚠️ **붙여넣은 뒤 반드시 한 번**: 앱 `[⚙️ 관리] → 관리 도구 → [🏦 혈비 계정을 혈맹운영비로 통일]`.
> 안 하면 다음 분배 때 운영비 계정이 하나 더 생깁니다.

**분배 산식이 바뀌었습니다 (핵심)**
- 멤버별 **분배비중(1~100%)** 도입. 멤버DB E열, 기본 100%
  ```
  운영비    = floor(총액 × 0.1)
  분배가능  = 총액 - 운영비
  기본1인당 = floor(분배가능 / 인원)
  각자      = floor(기본1인당 × 비중% / 100)
  잔여      = 분배가능 - Σ각자        → 전액 운영비로 귀속
  ```
  예) 10,000 / 10명, 그중 1명이 50%
  → 운영비 1,000 · 기본1인당 900 · 50% 대상자 450 · 남은 450도 운영비 → **운영비 1,450**
- **1/N 버림 나머지가 더 이상 특정 캐릭터(`TC무식`)에게 가지 않습니다.** 전액 운영비 귀속.
  `REMAINDER_NAME` 상수 폐지 — v9 이하에서 이미 분배된 행을 되돌릴 때만 옛 귀속처를 쓴다
- 불변식이 `운영비적립 + Σ각자 = 총액` 으로 바뀌었고, 검사도 그에 맞춰 다시 씀
  (5,069건 무작위 + 기준 예시를 숫자로 못박음)
- **[분배대기중] O열 "분배내역" 신설** — 분배 시점에 각자 실제로 받은 금액을 기록한다.
  비중은 언제든 바뀔 수 있어서, 되돌릴 때 "지금 비중"으로 다시 계산하면 잔액이 틀어진다.
  이제 정정·삭제는 그때 준 금액을 그대로 회수한다 (E2E 로 회귀 방지)

**명칭**
- 혈비 계정 `유일배분(혈비)` → **`혈맹운영비`**. 화면·시트·안내 문구 전부 교체
- 계정명은 코드 상수와 시트 표기가 **정확히 같아야** 하므로, 옮기는 일은 도구 하나로 처리한다
  (`renameFund` — 잔액·참여횟수를 그대로 끌고 간다. 이미 바뀌어 있으면 아무 일도 안 함)

**혈맹원**
- **아이디 변경 이력** — 변경 전 → 변경 후를 [작업기록]에서 뽑아 앱에서 본다 (병합 건은 표시)
- 멤버DB **F열 서버(01~12)**, **G열 한자표기** 추가. 앱 `[혈맹원 관리]` 에서 바로 수정
- 시즌 서버 이름 지정 (`[🗺️ 이번 시즌 서버 설정]`) — 헤더 시즌 칩에 함께 표시

**게시판 · 공지**
- `[📋 게시판]` 탭 신설. **일반 글은 혈맹원 누구나** 쓸 수 있다 (PIN 불필요)
- **공지는 관리자·마스터만.** 공지 여부는 요청 본문이 아니라 **쿠키로만** 판정한다 —
  본문의 `notice: true` 는 그 자체로 아무 힘이 없다 (검사로 강제)
- 최신 공지는 어느 탭에 있든 화면 맨 위 띠로 고정되고, 누르면 바로 펼쳐진다
- 앱에서 유일하게 인증 없이 열린 쓰기 경로라 한 기기당 10분 5건으로 제한

**연합**
- `[🤝 연합]` 탭 + `[연합]` 시트 신설. 혈맹 내부 잔액과 **완전히 분리된 장부**
- 서버 · 금액 · 아이템명 · 비중(1~100%) → `floor(금액 × 비중 ÷ 100)` 을 그 서버에 누적
- 인증샷은 **인원수만** 센다. 아이디는 판별하지 않는다 (연합 인원은 우리 멤버DB에 없으므로
  이름을 맞출 근거가 없다 — 규칙 7)

**권한 계층**
- **마스터관리자(개발자)** 추가. `MASTER_PIN` 환경변수로만 판정하며, 없으면 마스터 기능만 잠긴다
- 마스터 = 관리자가 할 수 있는 것 전부 + **앱 이름 변경** + **관리자 PIN 원격 교체**
- 바꾼 관리자 PIN 은 시트에 저장되고 그때부터 `ADMIN_PIN` 환경변수보다 우선한다 —
  관리자가 바뀌어도 Vercel 재배포 없이 앱에서 갈 수 있다. PIN 값은 어떤 로그에도 남기지 않는다

**중국어 전환 (실사용 반영으로 전면 재작업)**
- 처음엔 사전만 만들고 컴포넌트 대부분에 한국어가 그대로 박혀 있어서, 中文 으로 바꿔도
  거의 바뀌지 않았다. **화면 전체를 사전 기반으로 다시 썼다** (282개 문구 × 2개 언어)
- 언어 설정을 **React 컨텍스트**로 올렸다. prop 으로 넘기던 방식은 컴포넌트가 늘 때마다
  빠뜨리는 곳이 생긴다 — 실제로 그렇게 됐다
- 시트가 한국어로 내려주는 **관리 도구 이름·설명**도 도구 id 기준으로 중문 표기
- 재화 단위 '다이아' → '钻石', 시트 기본 앱 이름 '길드정산' → '血盟结算'
  (마스터가 직접 지은 이름은 데이터로 보고 그대로 둔다)
- 사람 이름·아이템명·게시글은 여전히 번역하지 않는다. 혈맹원 한자는 관리자가 눈으로
  확인해 넣은 값만 쓴다 — 기계가 이름을 지어내면 다른 사람으로 읽혀 다이아가 엉뚱한 곳으로
  간다 (규칙 7)
- 검사 2개 추가: 사전에 두 언어가 다 채워졌는가 · **컴포넌트에 한국어가 직접 박혀 있지 않은가**
- E2E 추가: 中文 으로 바꾼 뒤 헤더·탭·5개 화면을 돌며 **한글이 한 글자도 남지 않는지** 확인
  (사람 이름·아이템명 같은 데이터는 제외)

**로고**
- 다이아몬드 → **어두운 돌 바탕 + 각진 금색 W**. 게임의 공식 로고는 상표라 쓰지 않고,
  같은 분위기(흑금 · 거친 돌 · 뾰족하게 뻗은 획)를 좌표로 직접 그렸다
- 획은 캡슐이 아니라 **사다리꼴 다각형**이라 끝이 직선으로 잘리고 아래로 갈수록 뾰족해진다.
  뿔·봉우리는 획의 실제 모서리에서 세우고 살짝 겹쳐, 두께를 바꿔도 이음매가 비치지 않는다
- 바탕은 값 노이즈 4옥타브 + 비네팅으로 돌 질감을 만든다
- 헤더·테마색·매니페스트를 아이콘과 같은 계열(near-black + 금색)로 맞춤
- 직접 만든 이미지를 쓰고 싶으면 `public/icon-*.png` · `app/icon.png` · `app/apple-icon.png` 를
  같은 크기 PNG 로 덮어쓰면 된다 (코드 수정 불필요)

**버전 표시**
- 앱 제목 옆에 버전을 띄운다. 구글시트가 돌려준 `.gs` 버전과 다르면
  `v10.0 ⚠️ 시트 v9.1` 처럼 경고가 붙는다 — 코드는 붙여넣었는데 **재배포를 안 한**
  상태를 화면에서 바로 알아챌 수 있다 (실제로 겪은 상황)
- 버전은 `.gs` · 파일명 · `package.json` · `lib/version.ts` 네 곳에서 같아야 하고,
  검사가 이를 강제한다 (앱 쪽 값이 틀리면 멀쩡한 배포에도 경고가 뜨므로)

**검사**
- `verify:gs` 22 → **27건**. `app/api` 라우트까지 본다:
  관리자 핸들러가 전부 인증으로 시작하는가 · 마스터 라우트가 `requireMaster` 인가 ·
  공지 권한을 쿠키로 정하는가 · 라우트가 `confirm` 값을 임의로 채우지 않는가
- E2E 45 → **61건**. 비중 분배·되돌리기 정확성·게시판 권한·연합 검증·마스터 PIN 우선순위·
  버전 표시와 불일치 경고 포함

---

## v9.2 — 지난 시즌 조회 + 시즌 번호 자가보정

**시즌 번호가 틀리던 문제**
- 시즌 번호를 `SEASON_NUM` 문서 속성 하나에만 의존하고 있었다. 이 속성은 파일을 새로
  만들거나 스크립트를 갈아끼우면 따라오지 않아서, **실제로는 시즌 3인데 앱에 "시즌 1"로
  표시**됐다 (실사용에서 발견)
- `_currentSeason(ss)` 신설 — 속성값에서 출발하되 존재하는 `[시즌N]` 시트만큼 앞으로 민다.
  시트는 데이터와 함께 옮겨지므로 이쪽이 진실에 더 가깝다. 붙여넣기만 하면 스스로 맞춰진다
- 검사 추가: 시즌1·시즌2가 있으면 3을 돌려주는지 실제로 실행해 확인하고,
  `SEASON_NUM` 을 직접 읽는 곳이 헬퍼 하나뿐인지 강제

**가져오기에서 시즌 아카이브가 누락되던 문제**
- `[📥 기존 파일에서 가져오기]` 가 `[시즌N]` 시트를 옮기지 않았다. 옮기고 나면 지난 시즌
  기록이 사라지고 번호도 1로 되돌아가 보였다
- 이제 시즌 시트를 통째로 복사하고 시즌 번호도 함께 맞춘다 (옛 파일은 여전히 읽기만)

**지난 시즌 보기**
- 앱 상단 `[시즌 N ▾]` 을 누르면 보관된 시즌 목록 → 시즌을 고르면 상세
- 시즌 시트는 사람이 읽는 보고서 형식이라, 섹션 제목(💰📄📊💸)을 기준으로 잘라 표로 그린다.
  시트 형식이 바뀌어도 화면이 따라간다
- 조회 전용이라 길드원도 볼 수 있다. 끝난 시즌은 더 바뀌지 않으므로 넉넉히 캐시

---

## v9.1 — 🚨 긴급 보안 수정 (v9.0 이하 사용자는 반드시 갱신)

**무엇이 문제였나**

웹앱 배포 액세스는 "모든 사용자"여야 Vercel 서버가 호출할 수 있다. 그런데 그렇게 열면
`/exec` 주소를 아는 사람은 누구나 브라우저로 열어 구버전 화면(`_mobileHtml`)을 받을 수 있었고,
그 화면은 `google.script.run` 으로 `api_register` · `api_distribute` · `api_payout` 을 직접 불렀다.
토큰 검사는 `doPost` 에만 있으므로 **이 경로는 인증을 통째로 우회한다.**
즉 URL 하나만 알면 PIN 없이 등록·분배·지급이 가능했다.

**어떻게 고쳤나**

- `_mobileHtml` 완전 삭제 (191줄). 쓰기가 가능한 HTML 경로를 코드에서 없앤다 —
  주소를 비밀로 유지하는 것에 기대지 않는다
- `doGet` 기본 응답을 데이터가 전혀 없는 안내 페이지로 교체
- 읽기 전용 개인 조회(`?view=lookup`)는 유지 — Vercel 이 멈췄을 때의 비상 경로
- `verify:gs` 에 검사 추가: **doGet 이 내주는 모든 화면을 실제로 렌더링해**
  쓰기 함수 이름이 하나라도 들어 있으면 실패. `_mobileHtml` 부활도 함께 차단.
  일부러 넣어보는 변이 테스트로 두 경로 모두 잡히는 것을 확인

**토큰은 새어나가지 않았으므로 재발급은 필요 없다.** v9.1 을 붙여넣고
[배포 관리] → 새 버전으로 올리면 즉시 닫힌다.

---

## v9.0 — 모든 관리 기능을 앱에서 (PC 시트 불필요)

지금까지 "실수하면 크게 망가진다"는 이유로 PC 시트 메뉴에만 두었던 기능을 전부 앱으로 옮겼다.
막는 대신, **위험도에 비례하는 확인 절차**로 다룬다. 검사는 전부 서버(Apps Script)에서 하므로
앱을 고쳐도 뚫리지 않는다.

| 위험도 | 기능 | 서버가 요구하는 것 |
|---|---|---|
| 1 | 참여횟수 재계산 · 시트 정돈 · 디스코드 알림 | 없음 |
| 2 | 판매금액 정정 · 아이템 삭제 · 지급 취소 | `confirm === true` + 되돌릴 금액을 보여준 뒤 재호출 |
| 3 | 시즌 종료 · 데이터 이관 · 최초 설치 · 공장 초기화 | 정해진 문구를 정확히 입력 (`시즌종료`·`가져오기`·`설치`·`전부삭제`) |

**구조**
- `_uiAdapter(silent)` — UI 없는 환경에서도 기존 메뉴 함수를 그대로 실행. 로직을 복사해
  두 벌로 만들면 언젠가 어긋나므로, UI만 갈아끼우고 본문은 한 벌로 둔다.
  성공 메시지가 ✅ 로 시작하는 관례를 `_adapterResult` 가 성공·실패 판정에 쓴다
- `_reverseCheck` / `_reverseAmounts` — 정정과 삭제에 거의 같은 코드가 중복돼 있던
  되돌리기 로직을 한 곳으로 모음 (약 100줄 감소)
- `_correctCore` / `_deleteItemCore` / `_undoPayoutCore` 추출, PC 메뉴는 얇은 래퍼로
- `_toolRegistry()` + `api_runTool` — 도구를 하나의 API로 노출. 정의만 추가하면 앱에
  자동으로 나타나므로 앞으로 도구가 늘어도 화면 코드는 손대지 않는다

**안전장치**
- 이미 지급✓ 된 사람이 포함된 아이템은 정정·삭제가 **아예 시작되지 않는다**
- 되돌리기가 하나라도 실패하면 아이템 상태를 바꾸지 않는다 (19명 중 16명에서 중단됐던 사고 대응)
- 아이템 행을 지우기 전에 반드시 작업기록을 먼저 남긴다

**검증** (`.gs` 15 → 19항목, E2E 28 → 43건)
- 위험도 3 도구를 **실제로 실행**해 5가지 잘못된 입력이 전부 막히는지 확인
- UI 어댑터가 ✅/❌/⚠️ 를 정확히 구분하고, prompt 를 조용히 통과시키지 않는지
- 정정·삭제·지급취소의 확인 게이트와 라우터의 엄격 전달
- 되돌리기 부분 실패 시 상태 변경·행 삭제·로깅의 순서 보장

**고친 것**
- 테스트가 앞선 테스트의 상태 변경에 의존하던 문제. 화면 테스트 전에 모의 시트를 초기화하고,
  시즌 종료 테스트가 실수로 진짜 시즌을 끝내던 케이스를 제거

---

## v8.2 — 앱에서 혈맹원 추가 · 탈퇴

- `[➕ 혈맹원 추가]` — 신규 가입자를 앱에서 등록. 멤버DB에 넣고 `_syncMembers` 가
  참여자현황·잔액현황 행까지 자동 생성. 중복(_normName 기준)·최대 인원(50명)·혈비 계정 차단
- `[➖ 탈퇴 처리]` — **지우는 기능이 아니다.** 멤버DB에서만 빼고, 잔액이나 참여 이력이
  남아 있으면 잔액현황에 `이름 (미등록)` 회색으로 보존한다. 전부 0일 때만 행을 지운다
  (유령 행 방지). 분배대기중·지급기록·작업기록은 건드리지 않는다
- 되돌리기 어려운 상황이면 한 번 더 물어본다:
  받지 않은 잔액이 남은 사람 / 분배 나머지가 귀속되는 사람(REMAINDER_NAME).
  빼고 나면 이후 분배에서 나머지가 갈 곳이 없어지므로 함께 경고
- 검증: `.gs` 13 → 15항목(탈퇴 이력보존·추가 중복상한), E2E 22 → 28건

**고친 것**
- 테스트·촬영 스크립트가 하단 탭바와 목록의 `관리` 버튼을 구분하지 못해 엉뚱한 곳을
  누르던 문제 (`.nav button` / `.row button` 으로 범위 한정)
- 이전 실행이 남긴 서버가 포트를 잡고 있으면 옛 빌드를 내주는 바람에 "청크를 못 찾는다"는
  엉뚱한 오류로 테스트가 무더기로 깨지던 문제 → E2E 시작 전에 포트를 확인하고 분명히 알림

---

## v8.1 — 쓰던 시트를 멈추지 않고 옮기기 · 앱에서 아이디 변경

**새 파일로 이전 (기존 파일 무중단)**
- `[📥 기존 길드정산 파일에서 가져오기]` 신설. 새 파일에 v8.1을 설치해 앱까지 붙여
  충분히 확인한 뒤, 준비가 끝났을 때 한 번에 데이터를 옮긴다
- 가져오는 것: 멤버DB(게임표시명 포함) · 잔액현황 · 분배대기중 · 지급기록 · 작업기록
- **기존 파일은 읽기만 한다** — 검사 스크립트가 옛 파일에 대한 쓰기 호출 0건을 강제
- **덮어쓰기 방식**이라 여러 번 실행해도 결과가 같다. 합산 방식이면 두 번 실행할 때
  잔액이 불어난다. 다만 새 파일에서 정산을 시작한 뒤 재실행하면 그 작업이 사라지므로,
  가져온 날짜를 기록해두고 확인 팝업에서 경고한다
- 멤버DB에 없는 옛 이름도 잔액이 남아 있으면 버리지 않고 보존

**앱에서 혈맹원 아이디 변경**
- `[⚙️ 관리] → 혈맹원 아이디 관리` — 게임에서 이름을 바꾼 사람을 앱에서 바로 수정
- 잔액·참여횟수가 새 이름으로 그대로 승계
- 이미 있는 이름으로 바꾸면 두 계정이 합쳐지므로, 서버가 `needsConfirm` 을 돌려주고
  앱이 **양쪽 잔액을 보여주며 한 번 더 확인**받은 뒤에만 진행 (남의 잔액을 실수로
  흡수하는 사고 방지). 혈비 계정은 앱에서 변경 불가
- 모든 변경은 `[작업기록]` 시트에 영구 기록
- `_renameCore` 로 멤버DB 갱신과 잔액 승계를 한 곳에 묶어, PC 메뉴와 앱이 같은 코어를 쓴다

**검증 추가** (11 → 13항목, E2E 17 → 22건)
- 아이디 변경: 병합 재확인 절차·혈비 보호·라우터의 `confirmMerge` 엄격 전달
- 가져오기: 옛 파일에 대한 쓰기 호출이 하나도 없는지 정적 검사
- E2E: 명단 권한 · 잔액 승계 · 병합 되묻기 · 혈비 거부

---

## v8.0 — 스마트폰 앱(PWA)으로 전환

구글시트 안에 HTML을 문자열로 넣어두던 구조를 걷어내고, 화면과 데이터를 분리했습니다.

**구글시트 (Apps Script)**
- `doPost` JSON API 신설. 액션 8종(`ping`/`state`/`members`/`lookup`/`register`/`distribute`/`payout`/`photo`)
- 전부 기존 `api_*` 코어를 그대로 호출 — **분배·지급 산식은 한 줄도 바뀌지 않았습니다**
- 스크립트 속성 `API_TOKEN` 상수시간 검증. `[🔑 웹 API 토큰]` 메뉴로 발급·재발급
- 쓰기 3종은 `LockService` 로 직렬화 (동시 클릭에도 잔액이 꼬이지 않음)
- 분배 정정·아이템 삭제·시즌 종료·공장 초기화는 API에 **의도적으로 미노출**
- 기존 `doGet` 웹앱은 백업 경로로 존치

**앱 (Next.js on Vercel)**
- 하단 4탭(잔액·아이템·내 정보·관리), 라이트/다크, 홈 화면 설치
- 조회는 누구나 / 등록·분배·지급은 PIN을 넣은 관리자만
- 관리자 세션은 `SESSION_SECRET` 서명 httpOnly 쿠키(30일), PIN 시도 10분 10회 제한
- `GAS_TOKEN` 은 `server-only` 로 브라우저 번들 유입 차단
- 사진 OCR은 기존과 같은 보정값(1600px · contrast 160%)으로 전처리
- 서비스워커는 화면 껍데기만 캐시하고 `/api` 응답은 저장하지 않음
- `/api/health` 로 설정 누락·시트 연결을 즉시 진단

**공유**
- `[⚙️ 관리]` 탭에 QR·링크 복사·공유 시트. QR은 앱 안에서 생성(외부 서비스에 주소를 넘기지 않음)

**유지관리**
- `CLAUDE.md` — 어디를 고쳐야 하는지, 절대 규칙, 자주 하는 실수
- `npm run verify` — 타입·린트·`.gs` 검사·빌드·E2E를 한 번에
- `npm run verify:gs` — `.gs` 11개 항목 (구문·중복·라우터 노출면·락·**분배 산식 이중구현 5,000건 대조**·사용안내·보호훅)
- `npm run dev:mock` — 진짜 시트 없이 로컬 실행
- `npm run e2e` — 권한 경계(미인증·오PIN·쿠키 위조 401) + 화면 흐름 17건
- GitHub Actions로 push마다 자동 검증

**고친 것**
- `/api/state` 가 **실패 응답까지 캐시**하던 문제 — 구글시트가 한 번 삐끗하면 8초 동안
  모든 사용자가 오류를 보게 됩니다. 이제 성공만 캐시합니다. (E2E 작성 중 발견)

---

## v7.4 이전

`references/architecture.md`(guild-manager 스킬) 의 버전 이력을 참고하세요.
v2(원형) → v4.0(모바일 웹앱) → v5.0(선등록·후분배) → v6.0(OCR 자동 감지) →
v7.0(참여횟수 분리) → v7.4(대시보드·자동알림·개인조회) 로 이어집니다.

<!--
배포 메모 (v10.8.1)

Vercel 은 작업 브랜치에서 이미 빌드한 커밋 SHA 를 main 에서 다시 받으면
프로덕션 빌드를 건너뛴다. 브랜치를 push 한 직후 같은 SHA 를 main 으로
fast-forward 하면 프리뷰만 생기고 프로덕션은 옛 버전에 머문다 —
실제로 두 번 겪었다.

앞으로는 **main 을 먼저 push** 하고 작업 브랜치를 나중에 맞춘다.
-->
