# 변경 이력

버전 규칙: 구조 변경·큰 기능은 정수(v8 → v9), 작은 개선은 소수점(v8.0 → v8.1).
`apps-script/GuildManager_v*.gs` 의 `VERSION` 상수와 항상 일치해야 합니다.

---

## v9.0 — 모든 관리 기능을 앱에서 (PC 시트 불필요)

지금까지 "실수하면 크게 망가진다"는 이유로 PC 시트 메뉴에만 두었던 기능을 전부 앱으로 옮겼다.
막는 대신, **위험도에 비례하는 확인 절차**로 다룬다. 검사는 전부 서버(Apps Script)에서 하므로
앱을 고쳐도 뚫리지 않는다.

| 위험도 | 기능 | 서버가 요구하는 것 |
|---|---|---|
| 1 | 참여횟수 재계산 · 시트 정돈 · 디스코드 알림 | 없음 |
| 2 | 판매금액 정정 · 아이템 삭제 · 지급 취소 | `confirm === true` + 되돌릴 금액을 보여준 뒤 재호출 |
| 3 | 시즌 종료 · 데이터 이관 · 최초 설치 · 공장 초기화 | 정해진 문구를 정확히 입력 (`시즌종료`·`가져오기`·`설치`·`전부삭제`) |

**구조**
- `_uiAdapter(silent)` — UI 없는 환경에서도 기존 메뉴 함수를 그대로 실행. 로직을 복사해
  두 벌로 만들면 언젠가 어긋나므로, UI만 갈아끼우고 본문은 한 벌로 둔다.
  성공 메시지가 ✅ 로 시작하는 관례를 `_adapterResult` 가 성공·실패 판정에 쓴다
- `_reverseCheck` / `_reverseAmounts` — 정정과 삭제에 거의 같은 코드가 중복돼 있던
  되돌리기 로직을 한 곳으로 모음 (약 100줄 감소)
- `_correctCore` / `_deleteItemCore` / `_undoPayoutCore` 추출, PC 메뉴는 얇은 래퍼로
- `_toolRegistry()` + `api_runTool` — 도구를 하나의 API로 노출. 정의만 추가하면 앱에
  자동으로 나타나므로 앞으로 도구가 늘어도 화면 코드는 손대지 않는다

**안전장치**
- 이미 지급✓ 된 사람이 포함된 아이템은 정정·삭제가 **아예 시작되지 않는다**
- 되돌리기가 하나라도 실패하면 아이템 상태를 바꾸지 않는다 (19명 중 16명에서 중단됐던 사고 대응)
- 아이템 행을 지우기 전에 반드시 작업기록을 먼저 남긴다

**검증** (`.gs` 15 → 19항목, E2E 28 → 43건)
- 위험도 3 도구를 **실제로 실행**해 5가지 잘못된 입력이 전부 막히는지 확인
- UI 어댑터가 ✅/❌/⚠️ 를 정확히 구분하고, prompt 를 조용히 통과시키지 않는지
- 정정·삭제·지급취소의 확인 게이트와 라우터의 엄격 전달
- 되돌리기 부분 실패 시 상태 변경·행 삭제·로깅의 순서 보장

**고친 것**
- 테스트가 앞선 테스트의 상태 변경에 의존하던 문제. 화면 테스트 전에 모의 시트를 초기화하고,
  시즌 종료 테스트가 실수로 진짜 시즌을 끝내던 케이스를 제거

---

## v8.2 — 앱에서 혈맹원 추가 · 탈퇴

- `[➕ 혈맹원 추가]` — 신규 가입자를 앱에서 등록. 멤버DB에 넣고 `_syncMembers` 가
  참여자현황·잔액현황 행까지 자동 생성. 중복(_normName 기준)·최대 인원(50명)·혈비 계정 차단
- `[➖ 탈퇴 처리]` — **지우는 기능이 아니다.** 멤버DB에서만 빼고, 잔액이나 참여 이력이
  남아 있으면 잔액현황에 `이름 (미등록)` 회색으로 보존한다. 전부 0일 때만 행을 지운다
  (유령 행 방지). 분배대기중·지급기록·작업기록은 건드리지 않는다
- 되돌리기 어려운 상황이면 한 번 더 물어본다:
  받지 않은 잔액이 남은 사람 / 분배 나머지가 귀속되는 사람(REMAINDER_NAME).
  빼고 나면 이후 분배에서 나머지가 갈 곳이 없어지므로 함께 경고
- 검증: `.gs` 13 → 15항목(탈퇴 이력보존·추가 중복상한), E2E 22 → 28건

**고친 것**
- 테스트·촬영 스크립트가 하단 탭바와 목록의 `관리` 버튼을 구분하지 못해 엉뚱한 곳을
  누르던 문제 (`.nav button` / `.row button` 으로 범위 한정)
- 이전 실행이 남긴 서버가 포트를 잡고 있으면 옛 빌드를 내주는 바람에 "청크를 못 찾는다"는
  엉뚱한 오류로 테스트가 무더기로 깨지던 문제 → E2E 시작 전에 포트를 확인하고 분명히 알림

---

## v8.1 — 쓰던 시트를 멈추지 않고 옮기기 · 앱에서 아이디 변경

**새 파일로 이전 (기존 파일 무중단)**
- `[📥 기존 길드정산 파일에서 가져오기]` 신설. 새 파일에 v8.1을 설치해 앱까지 붙여
  충분히 확인한 뒤, 준비가 끝났을 때 한 번에 데이터를 옮긴다
- 가져오는 것: 멤버DB(게임표시명 포함) · 잔액현황 · 분배대기중 · 지급기록 · 작업기록
- **기존 파일은 읽기만 한다** — 검사 스크립트가 옛 파일에 대한 쓰기 호출 0건을 강제
- **덮어쓰기 방식**이라 여러 번 실행해도 결과가 같다. 합산 방식이면 두 번 실행할 때
  잔액이 불어난다. 다만 새 파일에서 정산을 시작한 뒤 재실행하면 그 작업이 사라지므로,
  가져온 날짜를 기록해두고 확인 팝업에서 경고한다
- 멤버DB에 없는 옛 이름도 잔액이 남아 있으면 버리지 않고 보존

**앱에서 혈맹원 아이디 변경**
- `[⚙️ 관리] → 혈맹원 아이디 관리` — 게임에서 이름을 바꾼 사람을 앱에서 바로 수정
- 잔액·참여횟수가 새 이름으로 그대로 승계
- 이미 있는 이름으로 바꾸면 두 계정이 합쳐지므로, 서버가 `needsConfirm` 을 돌려주고
  앱이 **양쪽 잔액을 보여주며 한 번 더 확인**받은 뒤에만 진행 (남의 잔액을 실수로
  흡수하는 사고 방지). 혈비 계정은 앱에서 변경 불가
- 모든 변경은 `[작업기록]` 시트에 영구 기록
- `_renameCore` 로 멤버DB 갱신과 잔액 승계를 한 곳에 묶어, PC 메뉴와 앱이 같은 코어를 쓴다

**검증 추가** (11 → 13항목, E2E 17 → 22건)
- 아이디 변경: 병합 재확인 절차·혈비 보호·라우터의 `confirmMerge` 엄격 전달
- 가져오기: 옛 파일에 대한 쓰기 호출이 하나도 없는지 정적 검사
- E2E: 명단 권한 · 잔액 승계 · 병합 되묻기 · 혈비 거부

---

## v8.0 — 스마트폰 앱(PWA)으로 전환

구글시트 안에 HTML을 문자열로 넣어두던 구조를 걷어내고, 화면과 데이터를 분리했습니다.

**구글시트 (Apps Script)**
- `doPost` JSON API 신설. 액션 8종(`ping`/`state`/`members`/`lookup`/`register`/`distribute`/`payout`/`photo`)
- 전부 기존 `api_*` 코어를 그대로 호출 — **분배·지급 산식은 한 줄도 바뀌지 않았습니다**
- 스크립트 속성 `API_TOKEN` 상수시간 검증. `[🔑 웹 API 토큰]` 메뉴로 발급·재발급
- 쓰기 3종은 `LockService` 로 직렬화 (동시 클릭에도 잔액이 꼬이지 않음)
- 분배 정정·아이템 삭제·시즌 종료·공장 초기화는 API에 **의도적으로 미노출**
- 기존 `doGet` 웹앱은 백업 경로로 존치

**앱 (Next.js on Vercel)**
- 하단 4탭(잔액·아이템·내 정보·관리), 라이트/다크, 홈 화면 설치
- 조회는 누구나 / 등록·분배·지급은 PIN을 넣은 관리자만
- 관리자 세션은 `SESSION_SECRET` 서명 httpOnly 쿠키(30일), PIN 시도 10분 10회 제한
- `GAS_TOKEN` 은 `server-only` 로 브라우저 번들 유입 차단
- 사진 OCR은 기존과 같은 보정값(1600px · contrast 160%)으로 전처리
- 서비스워커는 화면 껍데기만 캐시하고 `/api` 응답은 저장하지 않음
- `/api/health` 로 설정 누락·시트 연결을 즉시 진단

**공유**
- `[⚙️ 관리]` 탭에 QR·링크 복사·공유 시트. QR은 앱 안에서 생성(외부 서비스에 주소를 넘기지 않음)

**유지관리**
- `CLAUDE.md` — 어디를 고쳐야 하는지, 절대 규칙, 자주 하는 실수
- `npm run verify` — 타입·린트·`.gs` 검사·빌드·E2E를 한 번에
- `npm run verify:gs` — `.gs` 11개 항목 (구문·중복·라우터 노출면·락·**분배 산식 이중구현 5,000건 대조**·사용안내·보호훅)
- `npm run dev:mock` — 진짜 시트 없이 로컬 실행
- `npm run e2e` — 권한 경계(미인증·오PIN·쿠키 위조 401) + 화면 흐름 17건
- GitHub Actions로 push마다 자동 검증

**고친 것**
- `/api/state` 가 **실패 응답까지 캐시**하던 문제 — 구글시트가 한 번 삐끗하면 8초 동안
  모든 사용자가 오류를 보게 됩니다. 이제 성공만 캐시합니다. (E2E 작성 중 발견)

---

## v7.4 이전

`references/architecture.md`(guild-manager 스킬) 의 버전 이력을 참고하세요.
v2(원형) → v4.0(모바일 웹앱) → v5.0(선등록·후분배) → v6.0(OCR 자동 감지) →
v7.0(참여횟수 분리) → v7.4(대시보드·자동알림·개인조회) 로 이어집니다.
