# 길드정산 — 작업 지침

이 저장소를 고치기 전에 이 문서를 읽으세요. 아래 규칙들은 전부 **실제 사고에서 나온 것**입니다.

---

## 무엇을 고쳐야 하는가 — 먼저 이것부터 판단

요청이 들어오면 **화면 얘기인지 정산 로직 얘기인지** 먼저 가릅니다. 배포 경로가 완전히 다릅니다.

| 요청 예시 | 고칠 곳 | 배포 |
|---|---|---|
| "잔액 탭 정렬 바꿔줘", "버튼 색", "탭 추가" | `components/`, `app/globals.css` | push → Vercel 자동 (1~2분) |
| "혈비 비율 바꿔줘", "분배 규칙", "새 시트 추가" | `apps-script/GuildManager_v11_7.gs` | **사용자가 직접** 붙여넣고 재배포 |
| "API에 기능 추가" | `.gs` 의 `_apiRoute` + `app/api/` | 양쪽 다 |

> ⚠️ `.gs` 의 `_lookupHtml`(읽기 전용 비상 조회)은 사용자가 실제로 보는 앱이 **아닙니다.**
> 화면 요청을 받고 여기를 고치면 아무 일도 일어나지 않습니다. `components/` 를 고치세요.
>
> 🚨 **`doGet` 이 내주는 화면에는 쓰기 기능을 절대 넣지 마세요.**
> 웹앱 액세스는 "모든 사용자"여야 Vercel 이 호출할 수 있고, 그래서 `/exec` 주소를 아는
> 누구나 그 화면을 받습니다. 거기서 `google.script.run` 으로 부르는 호출은 `doPost` 의
> 토큰 검사를 **거치지 않습니다.** v9.0 에서 실제로 이 경로로 PIN 없이 등록·분배·지급이
> 가능했고, v9.1 에서 해당 화면(`_mobileHtml`)을 삭제해 막았습니다.
> `npm run verify:gs` 가 이 규칙을 강제합니다.

`.gs` 를 고쳤다면 응답에 반드시 이 두 줄을 넣으세요 — 안 하면 사용자는 반영이 안 된 줄 모릅니다:
1. Apps Script 편집기에 새 `.gs` 전체를 붙여넣기
2. **[배포] → [배포 관리] → 편집(✏️) → 새 버전** (❌ [새 배포]로 하면 URL이 바뀝니다)

---

## 이 저장소 하나가 여러 길드의 앱을 만듭니다 — `main` push 는 전부에게 나갑니다

`1 배포 = 1 길드` 입니다. 코드는 한 벌이고 길드마다 `GAS_URL`(구글시트)만 다릅니다.
그래서 **같은 저장소를 보는 Vercel 프로젝트가 여럿 생길 수 있습니다.**

```
                         ┌─→ [Vercel 프로젝트 A] → 우리 길드 시트
rhrjsgks-jarvis/guild ───┤
        main             └─→ [Vercel 프로젝트 B] → 다른 길드 시트
```

**도메인·환경변수·시트가 달라도 배포는 함께 나갑니다.** Vercel 은 push 를 받으면
그 저장소를 구독한 **모든 프로젝트**를 빌드합니다. 도메인이 다르다고 막아주지 않습니다.

실제로 일어난 일: 다른 길드에게 준 앱이 우리 릴리스를 따라 v10.8.5~10.8.9 로
같이 올라갔습니다. **데이터가 섞이지는 않지만**(시트가 별개), 그쪽은 원하지도 않은
업데이트를 받고 시트 버전이 어긋나 경고가 뜹니다. 반대 방향이 더 위험합니다 —
남의 길드용 변경이 `main` 에 들어오면 **우리 실사용 앱이 그대로 바뀝니다.**

지켜야 할 것:

- 새 길드에 앱을 줄 때는 **저장소를 따로** 두는 것이 원칙입니다. 같은 저장소를
  쓰겠다면 그 프로젝트의 **Production Branch 를 `main` 이 아닌 것으로** 바꾸거나
  **Git 연결을 끊어** 두세요 (Vercel → 프로젝트 → Settings → Git)
- `main` 에 올리기 전에 **이 저장소를 보는 프로젝트가 몇 개인지** 확인하세요.
  `list_projects` → 각 프로젝트의 배포 기록에 `githubRepo` 가 찍혀 있습니다
- 배포가 두 배로 나가면 **Vercel 하루 배포 한도**도 두 배로 씁니다

## 구조

```
[폰 PWA]  →  [Vercel 서버 라우트]  →  [Apps Script doPost]  →  [구글시트]
  화면          토큰 보관 · PIN 인증        기존 정산 코어           진짜 데이터
```

브라우저는 **절대** Apps Script를 직접 부르지 않습니다. 두 가지 이유가 다 중요합니다.
- Apps Script는 CORS 프리플라이트에 응답하지 않아 브라우저 POST가 막힙니다
- `GAS_TOKEN` 이 번들에 실려 노출됩니다 (`lib/gas.ts` 의 `server-only` 가 이걸 강제로 막습니다)

### 권한 경계

| | 조회 | 게시판 글쓰기 | 등록·분배·지급 | 아이템 수정 | 정정·삭제·지급취소 | 되돌릴 수 없는 도구 | 앱이름·관리자PIN |
|---|---|---|---|---|---|---|---|
| 아무나 (링크만) | ✅ | ✅ | ❌ | ❌ | ❌ | ❌ | ❌ |
| 관리자 (`ADMIN_PIN`) | ✅ | ✅ + 공지 | ✅ | ✅ (v11.7) | ❌ | ❌ | ❌ |
| 마스터관리자 (`MASTER_PIN`) | ✅ | ✅ + 공지 | ✅ | ✅ | ✅ | ✅ | ✅ |

**마스터는 관리자의 상위 집합입니다** — 관리자가 할 수 있는 것은 마스터도 전부 할 수 있습니다.
반대는 성립하지 않습니다. 나누는 기준은 하나입니다: **되돌릴 수 있는가.**

| 마스터 전용 | 왜 |
|---|---|
| 이미 **정산된** 연합 건 수정 (v11.3) | 고칠 때마다 혈맹운영비 잔액이 실제로 움직인다 |
| 위험도 3 도구 (시즌종료·이관·설치·초기화) | 관리자가 실수로 눌러도 복구할 방법이 없다 |
| 정정(금액만)·미분배 삭제 | 이미 끝난 분배를 되돌리는 작업 — 관리자가 잘못 만진 것을 바로잡는 자리 |
| 지급취소 | 위와 같은 성격 |
| 앱이름·관리자PIN 변경 | 관리자 자신의 권한을 바꾸는 일 |

**조회는 관리자에게 열어둡니다** — 정정 미리보기, 지급기록 조회 등.
무엇이 잘못됐는지는 알아야 마스터에게 정확히 알릴 수 있습니다.

**아이템 수정은 관리자에게 열려 있습니다** (v11.7, `/api/admin/item`). 분배가 끝난 건도
아이템명·참여자·판매금액을 고칩니다 — 그 대신 **분배완료 건의 삭제를 없앴습니다**(규칙 2-2).
여는 근거는 "안전해서"가 아니라 **되돌릴 수 있어서**입니다: 수정은 분배 시점 스냅샷으로
회수한 뒤 다시 나누고 **기록을 지우지 않으므로**, 잘못 고쳤으면 다시 고치면 됩니다.
잘못 나눈 것을 바로잡을 길이 관리자에게 하나도 없으면, 고칠 사람을 기다리는 동안
잔액이 틀린 채로 남습니다.

도구의 등급 판정은 **시트가** 합니다 (`_toolNeedsMaster`, 위험도 3 이상).
라우트는 앱이 보낸 값이 아니라 시트에서 받은 도구 목록으로 판정하고,
목록을 못 받아오면 **막는 쪽**으로 갑니다 — 되돌릴 수 없는 작업을
"확인이 안 되니 일단 통과"시키는 것이 훨씬 위험합니다.

게시판 일반 글쓰기는 **앱에서 유일하게 인증 없이 열린 쓰기 경로**입니다.
그래서 ① 정산 데이터에 접근하지 않고 ② 한 기기당 10분 5건으로 제한하며
③ **공지 여부는 요청 본문이 아니라 쿠키로만** 정합니다 — 본문의 `notice: true` 는
그 자체로 아무 힘이 없습니다. 비슷한 공개 쓰기를 새로 만들 때 이 세 가지를 그대로 지키세요.

새 API 라우트를 만들 때, **쓰기 작업이면 첫 줄이 반드시** `const denied = await requireAdmin(); if (denied) return denied;` 입니다.
마스터 전용이면 `requireMaster()`. `app/api/admin/`(관리자) · `app/api/master/`(마스터) 아래에
두면 경로만으로 의도가 드러나서 리뷰가 쉬워집니다. `npm run verify:gs` 가
`app/api/admin/` 아래 모든 핸들러에 인증 호출이 있는지 실제로 확인합니다.

---

## 절대 규칙

### 1. 분배 산식은 두 곳에 있다 — 항상 같이 고친다

| 위치 | 역할 |
|---|---|
| `.gs` 의 `_calcSplit` | 실제로 다이아를 움직이는 계산 |
| `lib/client.ts` 의 `calcSplit` | 확인 화면에 보여주는 미리보기 |

연합 적립액도 같은 규칙입니다 — `.gs` 의 `_calcAlliance` 와 `lib/client.ts` 의 `calcAlliance`.

한쪽만 고치면 **사용자가 확인 버튼을 누를 때 본 숫자와 실제 결과가 달라집니다.**
`npm run verify:gs` 가 5,000건 무작위 대조로 이걸 잡지만, 애초에 같이 고치세요.

산식 자체 (v10.0):
```
운영비    = floor(총액 × 0.1)                  → FUND_NAME('혈맹운영비')
분배가능  = 총액 - 운영비
기본1인당 = floor(분배가능 / 인원)               ← 비중 100% 기준
각자      = floor(기본1인당 × 비중% / 100)      ← 멤버DB E열, 기본 100
잔여      = 분배가능 - Σ각자                    → 전액 운영비로 추가 귀속
```
**불변식: `운영비적립 + Σ각자 = 총액`** — 다이아는 사라지지도 생겨나지도 않습니다.

기준 예시 (사용자가 확인해준 값 — 검사에 숫자로 박혀 있습니다):
> 1만 다이아를 10명이 나누고 그중 1명이 50% → 운영비 1,000 · 기본1인당 900 ·
> 50% 대상자 450 · 남은 450도 운영비 → **운영비 1,450**

### 2. 잔여분은 참여자에게도, 특정 캐릭터에게도 가지 않는다

참여자는 **자기 비중만큼만** 받습니다. 버림으로 남은 몫이나 비중 미달분을 누군가에게
얹으면 그 사람만 금액이 달라져 버그로 오인됩니다 (실제로 있었던 사고). v9까지는
남은 몫이 `REMAINDER_NAME`(군주 캐릭터)에게 갔지만, **v10부터 전액 혈맹운영비로 귀속**됩니다.
잔여분 때문에 참여횟수를 올리지 않는 것은 그대로입니다.

### 2-1. 되돌릴 때는 "그때 준 금액"을 쓴다

비중은 언제든 바뀝니다. 정정·삭제 시점의 비중으로 다시 계산하면 실제로 준 금액과
어긋나 잔액이 틀어집니다. 그래서 `_distributeCore` 는 분배 시점 금액을
`[분배대기중]` **O열 "분배내역"** 에 남기고, `_reversalPlan` 이 그 값을 그대로 회수합니다.
(O열이 없는 v9 이하 행은 당시 산식 — 전원 동일 + 옛 귀속처 — 으로 복원합니다.)

새로 만드는 되돌리기 기능도 반드시 이 스냅샷을 쓰세요. 다시 계산하지 마세요.

**참여자까지 바꾸는 정정**(v11.2 `api_editItem` 의 분배완료 경로)은 순서가 전부입니다:
① 스냅샷으로 **회수를 끝내고** → ② 명단을 갈아끼우고 → ③ 새 금액으로 재분배.
①보다 ②를 먼저 하면 **실제로 준 사람에게서 못 빼고 안 받은 사람에게서 뺍니다.**
`npm run verify:gs` 가 두 코드의 순서를 실제로 비교합니다.

### 2-2. 분배된 기록은 지우지 않는다 — 고칠 때는 수정한다 (v11.7)

잔액은 되돌릴 수 있어도 **"그때 누가 얼마를 받았다"는 사실은 되돌릴 수 없습니다.**
행을 지우면 그것까지 사라집니다. "기록은 지워지지 않는다"는 이 시스템의 기본 약속이고
(규칙 5-2 가 탈퇴에 대해 말하는 것과 같은 이유), 분배 기록은 그중에서도 가장
지워지면 안 되는 것입니다.

| | 삭제 | 수정 |
|---|---|---|
| ⏳미분배 | ✅ 마스터 (아직 돈이 안 움직였다) | ✅ 관리자 이상 |
| ✅분배완료 | ❌ **없다** — `_deleteItemCore` 가 거부(`e.doneNoDelete`) | ✅ 관리자 이상 (스냅샷 회수 후 재분배) |

판정은 **시트가** 합니다. 화면에서 버튼만 감추면 라우트를 직접 부르는 길이 그대로
열려 있습니다 (규칙 5-3). 되돌리기가 필요한 작업(잘못된 금액·명단)은 전부
`api_editItem` → `_correctCore` 한 길을 지나므로, 회수 순서(규칙 2-1)도 한 곳에서 지켜집니다.

### 3. 참여횟수(CNT)와 다이아(PENDING)는 다른 사건에 속한다

- **CNT** — 등록(`_registerCore`) 시점에만 바뀜. "레이드 출석" 지표
- **PENDING** — 분배/정정 시점에만 바뀜. "금액" 지표

두 로직에서 서로의 필드를 건드리지 마세요. 예전에 분배할 때 CNT까지 같이 올렸다가
정정할 때 숫자가 흔들리는 문제가 있었습니다.

### 4. 이름 비교는 반드시 `_normName` 경유

`'잠단 (斬斷)'` 과 `'잠단(斬斷)'` 은 같은 사람입니다. 직접 `===` 로 비교하면 중복 행이 생깁니다.
`_normName` 은 공백과 `(미등록)` 을 지웁니다.

### 5. 위험한 기능은 "막는" 대신 "확인 강도"로 다룬다 (v9.0)

v9.0부터 **모든 관리 기능이 앱에 열려 있습니다.** PC 시트 메뉴 전용은 없습니다.
대신 위험도에 비례하는 확인 절차를 **서버에서** 강제합니다 — 앱을 고쳐도 뚫리지 않아야 합니다.

| 위험도 | 예 | 서버가 요구하는 것 |
|---|---|---|
| 1 | 참여횟수 재계산, 시트 정돈, 디스코드 설정 | 없음 (앱에서 한 번 확인) |
| 2 | 정정·삭제·지급취소, 아이디 변경, 탈퇴 | `confirm === true` + 구체적인 숫자를 보여준 뒤 재호출 |
| 3 | 시즌 종료, 데이터 이관, 최초 설치, 공장 초기화 | **정해진 문구를 정확히 입력** (`confirmText`) |

새 기능을 추가할 때는 `_toolRegistry()` 에 정의만 넣으면 앱에 자동으로 나타납니다.
화면 코드는 고치지 않아도 됩니다. `danger: 3` 이면 `confirm` 문구를 반드시 함께 지정하세요.

**절대 하지 말 것**: 서버 라우트에서 `confirm`/`confirmText` 를 임의로 채우는 것.
반드시 `=== true` / 사용자가 입력한 문자열 그대로 전달하세요. `npm run verify:gs` 가 이걸 검사합니다.

### 5-1. 되돌릴 수 없는 작업은 두 번 물어본다

두 곳에 이 패턴이 있습니다.

| 기능 | 되돌릴 수 없는 이유 | 플래그 |
|---|---|---|
| `api_renameMember` | 이미 있는 이름으로 바꾸면 두 사람 잔액이 합쳐짐 | `confirmMerge` |
| `api_removeMember` | 잔액이 남은 사람을 빼면 받지 못한 다이아가 남음 | `confirmRemove` |

서버가 플래그 없이는 거부하고 `needsConfirm` 을 돌려주면, 앱이 **구체적인 숫자를 보여준 뒤**
다시 호출합니다. 서버 라우트가 이 값을 임의로 `true` 로 만들면 안전장치가 통째로 무력화됩니다
— 반드시 `=== true` 로 엄격하게 전달하세요.

비슷한 성격의 기능(병합·삭제·덮어쓰기)을 새로 만들 때도 같은 2단계 패턴을 쓰고,
`scripts/verify-gs.mjs` 에 검사를 하나 추가하세요.

### 5-3. UI가 없는 곳에서도 같은 코드를 쓴다

PC 메뉴 함수를 앱에서도 부를 수 있게 `_uiAdapter(silent)` 가 있습니다.
로직을 복사해 두 벌로 만들면 언젠가 반드시 어긋나므로, **UI만 갈아끼우고 본문은 한 벌**로 둡니다.
silent 모드에서 `alert` 는 메시지를 모으고 항상 YES 를 돌려주며, `prompt` 는 예외를 던집니다
(값이 필요한 기능은 API 파라미터로 받아야 한다는 뜻).

성공 메시지는 **반드시 ✅ 로 시작**하세요. `_adapterResult` 가 이 관례로 성공·실패를 판정합니다.

### 5-2. 탈퇴는 "지우기"가 아니라 "명단에서 빼기"

`api_removeMember` 는 멤버DB에서만 빼고, 잔액·참여 이력이 있으면 잔액현황 행을
`이름 (미등록)` 회색으로 **보존**합니다. 전부 0일 때만 행을 지웁니다.
분배대기중·지급기록·작업기록은 절대 건드리지 않습니다 — "기록은 지워지지 않는다"는
이 시스템의 기본 약속입니다.

### 6. 여러 대상의 잔액을 도는 반복문은 개별 try/catch

Apps Script에는 트랜잭션이 없습니다. 실제로 19명 중 16명에서 실행이 끊겨 데이터가
어긋난 사고가 있었습니다. 반드시 (a) 대상별 개별 try/catch, (b) 하나라도 실패하면
상태 필드를 바꾸지 않음, (c) 반영/실패 내역을 사용자에게 정확히 알림.

### 6-1. 결과 메시지는 시트에서 번역하지 않는다 (v10.1)

앱은 한국어·中文·English 세 언어입니다. **문장을 시트에서 세 벌로 만들지 마세요.**

| 위치 | 역할 |
|---|---|
| `.gs` 의 `_rc(res, code, vars)` | "무슨 일이 있었는지"를 코드와 값으로 태그 |
| `lib/i18n.tsx` 의 `s.<code>` 항목 | 세 언어 문장 (`{item}`·`{amount}` 자리표시자) |
| `useT().srv(res, fallbackKey)` | 화면 언어로 문장 조립 |

```js
return _rc({ ok: true, msg: '✅ "' + item + '" 등록 완료' }, 'reg.ok', { item: item, n: 3 });
```

`msg` 는 **한국어 폴백으로 남겨두세요.** 사전에 없는 코드는 이 값이 그대로 나갑니다.

처음에는 시트에서 어구를 치환하는 방식을 썼다가 폐기했습니다 — 문장이 반쯤만 번역돼
`⚠️ 아직 받지 않 待发放 잔액이 9,299钻石 남아 있습니다` 같은 게 나왔습니다.

지켜야 할 것:
- **사람 이름·아이템명·계정명은 `vars` 로 그대로** 넘깁니다. 절대 번역하지 않습니다 (규칙 7과 같은 이유)
- **`danger: 3` 확인 문구는 어느 언어에서도 그대로**입니다. 번역되면 서버 비교가 영원히 실패합니다
- 새 결과 코드를 만들면 `lib/i18n.tsx` 에 `s.<code>` 를 세 언어로 **같은 커밋에서** 추가하세요.
  `npm run verify:gs` 가 양방향으로 대조합니다 (시트에만 있어도, 사전에만 있어도 실패)
- 모의 시트(`scripts/mock-sheet.mjs`)도 같은 `code`/`vars` 를 내보내야 E2E 언어 검사가 의미가 있습니다

### 6-4. 고유명사는 사전에 있는 것만 번역한다 (v11.4)

아이템·보스·서버 이름은 **사용자 데이터**라 기계가 번역하지 않습니다 (규칙 7). 대신
사람이 확인한 표기를 **[용어] 시트**에 모아두고, 앱은 그 표에 있는 말만 병기·자동완성합니다.

| | 규칙 |
|---|---|
| 저장되는 이름 | **언제나 국문**. 중국 혈맹원이 「龙之心」을 골라도 시트에는 '용의 심장' |
| 中文·English 빈칸 | "아직 확인 못 했다"는 뜻 — 국문 그대로 보여주고 **미입력이라고 표시** |
| 사전에 없는 이름 | **그대로 입력된다.** 자동완성이 입력을 막으면 새 아이템이 나올 때 등록이 멈춘다 |
| 그림 | 관리자가 넣은 주소만 (우리가 어디선가 긁어오지 않는다) |
| 권한 | 추가·수정·삭제는 **관리자 이상** — 틀려도 다이아가 안 움직이고 한 줄 고치면 끝난다 |

같은 국문이 두 줄이면 어느 것을 보여줄지 알 수 없으므로 시트가 거부합니다(`e.termDup`).
검색은 `lib/terms.ts` 의 `searchTerms` 한 벌이고, **세 언어를 모두** 봅니다 — 한 언어라도
빠지면 그 언어 사용자는 영영 못 찾습니다.

`npm run verify:gs` 가 이 규칙들을 전부 검사합니다.

### 6-2. 쓰기 직후의 조회만 캐시를 건너뛴다 (v10.1)

조회 라우트에는 짧은 메모리 캐시가 있습니다 (`lib/cache.ts`). 이 캐시는 **서버 인스턴스
메모리**에 있어서, 쓰기를 처리한 인스턴스에서 `invalidate` 를 해도 그다음 조회가
낡은 값을 든 **다른 인스턴스**로 갈 수 있습니다. 그래서 사용자가 "방금 넣었는데
숫자가 그대로"인 상황이 생겼습니다.

| 상황 | 호출 |
|---|---|
| 첫 로드 · 25초 주기 갱신 · 탭 복귀 | `/api/state` (캐시 사용) |
| 등록·분배·지급·글쓰기 **직후** | `/api/state?fresh=1` (캐시 건너뜀) |

- 새 조회 라우트를 만들면 `dropIfFresh(req, '<캐시키>')` 를 첫 줄에 넣으세요
- 새 쓰기 라우트는 `invalidate('<캐시키>')` 를 반드시 부르세요
- 상태를 바꾸는 쓰기라면 `callGas(..., { withState: true })` + `syncStateCache(res)` 를 쓰세요.
  시트가 같은 실행 안에서 상태를 읽어 보내주므로 **조회 왕복이 통째로 사라집니다** (규칙 6-3)
- 앱의 쓰기 완료 콜백은 `refreshNow`(= `refresh(true)`) 를 씁니다. `refresh()` 를 쓰면
  캐시된 옛 값을 다시 읽습니다
- **평소 조회까지 fresh 로 부르지 마세요.** 캐시가 없는 것과 같아져 Apps Script
  실행 할당량을 그대로 태웁니다. 특히 주기 갱신에 넣으면 안 됩니다

`npm run verify:gs` 가 이 네 가지를 전부 검사합니다 (TTL 10초 상한 포함).

### 6-3. 쓰기 응답에 상태를 실어 보낸다 (v10.2)

앱은 쓰기 뒤에 화면 숫자를 맞추려고 상태를 다시 읽어야 합니다. 그걸 두 번째 요청으로
하면 폰↔서버 왕복과 Apps Script 실행 준비 비용이 한 번 더 듭니다.

```
요청이 withState:true 를 보낸 쓰기 액션
  → .gs 의 _withState 가 같은 실행 안에서 api_getState() 를 읽어 res.state 에 담고
  → 라우트의 syncStateCache(res) 가 그 값을 상태 캐시에 넣고
  → App.tsx 의 refreshNow(res) 가 왕복 없이 화면에 반영한다
```

**절대 하지 말 것**: `_withState` 안의 상태 읽기 실패를 쓰기 실패로 바꾸는 것.
쓰기는 이미 끝났으므로, 실패로 보고하면 사용자가 같은 분배를 다시 실행합니다.
반드시 예외를 삼키고 쓰기 결과를 그대로 돌려주세요 — 앱이 알아서 한 번 더 조회합니다.

조회 액션·실패한 쓰기에는 붙이지 마세요 (시트를 두 번 읽게 됩니다).
`npm run verify:gs` 가 이 네 경우를 `_withState` 를 실제로 실행해서 검사합니다.

### 5-4. 일괄 작업은 "판정"과 "실행"을 나눈다 (v10.4)

여러 명·여러 건을 한 번에 바꾸는 기능은 잘못 넣으면 되돌리기가 아주 번거롭습니다.
그래서 반드시 두 단계로 나눕니다.

| 단계 | 함수 | 규칙 |
|---|---|---|
| ① 판정 | `api_analyzeMembers` | **아무것도 쓰지 않는다.** 상태만 매겨 돌려준다 |
| ② 실행 | `api_bulkAddMembers` | 관리자가 확정한 것만. `confirm === true` 필요 |

- **판정은 반드시 서버에서** 합니다. 앱에서만 검사하면 앱을 고쳐서 우회할 수 있습니다
- **애매한 것을 시스템이 확정하지 마세요.** 개명 후보는 제안만 하고 사람이 고릅니다 —
  잘못 이으면 두 사람 잔액이 합쳐집니다
- **읽어내지 못한 줄을 조용히 버리지 마세요.** '확인 필요'로 남겨 보여주세요.
  버리면 OCR 오독으로 누가 빠졌는지 관리자가 영영 모릅니다
- 이름을 바꾸는 것은 반드시 `_renameCore` 를 태우세요. 새로 추가하면 잔액·참여횟수·
  지난 시즌 기록이 0부터 다시 시작합니다
- 대상별 개별 try/catch (규칙 6). 실패 목록을 이름까지 돌려주세요

`npm run verify:gs` 가 판정 함수의 쓰기 호출을 실제로 찾아내 막습니다.

### 5-5. 시간표는 요일마다 한 줄, 못 읽은 값은 비운다 (v10.8)

[레이드] 시트는 `요일 · 시간 · 보스 · 비고` 입니다. 한 보스가 여러 요일에 나오면
**요일마다 한 줄**입니다. `월,수,금` 처럼 한 칸에 몰아 넣으면 "오늘 것만 보여주기"가
불가능해집니다 — 이 기능의 존재 이유가 그것입니다 (46종을 한 화면에 띄우면 폰에서 못 읽습니다).

| | 규칙 |
|---|---|
| 요일 | 1(월)~7(일). 시트에는 `월`·`화` 로 적고 `_normDay` 가 숫자로 바꿈. 못 읽으면 **0** |
| 시간 | `HH:MM` 24시간. `_normTime` 이 Date 셀·`20시20분`·`20:20` 을 하나로. 못 읽으면 **''** |
| 요일 판정 | **폰의 시계**로 (`getDay()` 0=일 → 7 로 변환). 서버에서 거르지 않는다 |

요일·시간·보스 중 하나라도 비면 `api_getRaid` 가 그 줄을 건너뜁니다. **지어내지 마세요** (규칙 7과 같은 이유).
요일을 서버에서 걸러 보내면 중국 접속(시차 1시간) 시 자정 근처에 엉뚱한 요일이 나오고,
요일 칩을 누를 때마다 왕복이 생깁니다.

편집은 **관리자 이상**입니다 (마스터 전용 아님) — 잘못 지워도 한 줄 다시 넣으면 끝이라
되돌릴 수 없는 작업이 아닙니다.

### 5-6. 공유 버튼은 게시판·관리에 붙이지 않는다 (v10.8)

`components/ShareBtn.tsx` 로 화면 내용을 **텍스트로** 내보냅니다 (스크린샷은 글자가 작아
읽히지 않고 검색도 안 됩니다). `navigator.share` → 클립보드 순으로 물러섭니다.

| 붙는 곳 | 안 붙는 곳 |
|---|---|
| 잔액 · 아이템 · 연합 · 레이드 · 내 정보 | **게시판 · 관리** |

게시판 글은 그 자체가 이미 공유물이고, 관리 탭에는 PIN·도구처럼 밖으로 나가면
안 되는 것이 섞여 있습니다. 공유 시트를 그냥 닫은 것(`AbortError`)은 **실패로 알리지 마세요** —
실패로 띄우면 사용자가 같은 걸 여러 번 보냅니다. `npm run verify:gs` 가 이 두 목록을 대조합니다.

### 5-7. 연합은 아이템 하나 = 여러 서버, 화면은 언제나 묶음 단위 (v11.0)

[연합] 시트는 **서버마다 한 줄**입니다. 같은 아이템을 여러 서버가 나눠 가지면
**K열 "묶음"** 값으로 이어지고, 앱·라우트는 `row` 가 아니라 **`group`** 을 주고받습니다.
한 줄씩 다루면 "3서버 중 한 서버만 정산된" 상태가 만들어지고, 그건 아무도 눈치채지 못합니다.

| | 규칙 |
|---|---|
| 산식 | 아이템 분배와 같은 모양 — 혈비 10% 뗀 뒤 **인원수 비례**, 잔여는 전액 혈비 |
| 혈비 | **혈맹운영비 잔액에 실제로 적립**됩니다 (L열에 기록). 삭제하면 회수합니다 |
| 개인 잔액 | 어느 단계에서도 건드리지 않습니다 — 연합 인원은 우리 멤버DB에 없습니다 |
| 참여횟수 | 건드리지 않습니다 (규칙 3). 연합은 우리 레이드 출석이 아닙니다 |
| 인증샷 | **선택**입니다. 없다고 등록을 막지 마세요. **줄마다 그 서버의 사진**입니다 (v11.3) |

**고치는 경로는 둘이고, 권한이 다릅니다** (v11.1).

| | 누가 | 무엇을 | 어떻게 |
|---|---|---|---|
| `api_editAlliance` (asMaster=true) | 마스터 | 아이템명·인원·**판매금액** | 정산된 건이면 `confirm === true` + 혈비를 **차액만** 조정 |
| `api_editAlliance` (asMaster=false) | 관리자 | **미정산 건만** — 아이템명·서버·인원·사진 | 정산된 건이면 시트가 거부 (`e.allyMasterOnly`) |
| `api_addAllianceServers` | 관리자 | **서버 줄 추가만** | 기존 줄은 못 고친다 — 그래서 관리자에게 열 수 있다 |

`asMaster` 는 **라우트가 고정**합니다 (`/api/master/…` = true, `/api/admin/…` = false).
앱이 보낸 값을 그대로 쓰면 누구나 마스터가 됩니다. 그리고 "정산된 건인가"의 판정은
**시트가** 합니다 — 라우트를 직접 부르는 길이 있으므로 라우트에서 끝내면 안 됩니다.

관리자에게 여는 쓰기는 **구조적으로 안전한 것만** 만드세요. "화면에서 안 보내니까
괜찮다"는 근거가 되지 않습니다 — 라우트를 직접 부르면 됩니다.
혈비를 다시 적립할 때는 **반드시 차액**입니다. 전액을 더하면 고칠 때마다 운영비가 불어납니다.

옛 기록(v10.9 이하)은 '묶음'이 비어 있습니다. 그 행은 **한 줄이 한 건**으로 읽고,
비중(%) 열은 지우지 말고 자리만 남겨두세요 — 지우면 옛 값을 다시 볼 수 없습니다.

### 5-8. 인증샷은 여러 장, 서버 줄마다, 저장 형태가 세 가지다 (v11.0 → v11.7)

`HYPERLINK` 은 링크를 **한 개만** 담습니다. 그래서 인증샷 칸은 세 형태를 씁니다.

| 언제 | 저장 |
|---|---|
| 서버를 안 고른 1장 | `=HYPERLINK("...","📷 보기")` — 예전 그대로 (시트에서 눌러 볼 수 있음) |
| 서버를 안 고른 2장 이상 | URL 을 **줄바꿈으로 나열**한 값 |
| 서버를 고른 사진 (v11.7) | 줄마다 `01\|주소` — 한 줄이 사진 한 장 |

**아이템 인증샷도 연합처럼 서버 줄마다 붙입니다** (v11.7). 연합은 시트가 서버마다
한 줄이라 공짜였지만, 아이템은 한 줄이라 칸 안에 서버를 함께 적습니다. 레이드 참여자가
한 화면에 다 안 들어가 서버 파티별로 나눠 찍는데, 사진을 한 자루에 담아 두면
**어느 서버 파티의 증거인지 영영 알 수 없습니다.**

| | 규칙 |
|---|---|
| 서버 | **안 골라도 된다** — 미지정(`sv: ''`)이다. 막으면 등록 자체가 막힌다 |
| 옛 기록 | 위 두 형태는 전부 미지정으로 읽힌다. **뒤늦게 서버를 짐작해 채우지 않는다** (규칙 7) |
| 01~12 가 아닌 값 | 미지정으로 (지어내지 않는다). 판정은 **시트**가 한다 (`_itemShotsCheck`) |
| 저장 | `_writeLedgerShots` 한 곳. **잇기만** 하고 지우는 길은 두지 않는다 |
| 나중에 더하기 | `api_addItemPhotos` — **관리자 이상**, 분배가 끝난 건에도 붙는다 |

`_photoList` 는 서버 표시를 떼고 **주소만** 돌려줍니다. 그래서 이 칸을 읽던 기존
코드(연합·데이터 이관·디스코드 알림)는 아무것도 안 고쳐도 그대로 돕니다.
어느 서버인지까지 필요한 곳만 `_shotList` / `_readLedgerShots` 를 씁니다.

**연합 시트는 예외로 언제나 원문 URL**만 씁니다 (v11.3). `api_getAlliance` 는 `getValues()`
로 읽는데, HYPERLINK 수식이면 계산 결과(`📷 보기`)만 돌아와 사진이 통째로 사라집니다 —
v11.1 의 [＋] 경로가 한 장일 때 수식으로 써서 실제로 그랬습니다. 쓰기는 `_writeAllyPhotos`
한 곳만 쓰고, 그 함수는 **잇기만** 합니다 (정정에서 사진을 지우는 길은 두지 않습니다).

읽을 때는 반드시 `_readLedgerPhotos(formula, display)` / `_photoList(cell)` 을 쓰세요.
수식만 보면 여러 장이 통째로 사라지고, 값만 보면 옛 한 장이 사라집니다.
시즌종료·데이터 이관은 수식이 없으면 값을 그대로 복사하므로 두 형태 다 옮겨갑니다.

### 5-9. 팝업은 바깥을 눌러서 닫히지 않는다 (v11.1)

폰에서는 시트가 화면을 거의 다 채웁니다. 스크롤하려다 가장자리를 스치면 그대로 닫혔고,
**입력하던 내용이 통째로 사라졌습니다.** 참여자를 스무 명 체크한 뒤라면 손해가 큽니다.

닫는 길은 **오른쪽 위 [✕]**, 아래쪽 버튼(취소·저장·닫기), 그리고 **폰 뒤로가기**(v11.2.1,
규칙 5-11)입니다. Esc 는 남겨둡니다.
새 모달을 만들 때 `components/Sheet.tsx` 를 쓰면 이 규칙이 자동으로 따라옵니다 —
배경에 `onClick` 을 다시 붙이지 마세요. `npm run verify:gs` 가 검사합니다.

### 5-11. 화면은 홈 아이콘에서 열고, 뒤로가기는 앱을 벗어나지 않는다 (v11.2.1)

**하단 탭바는 없습니다.** 첫 화면은 **홈**이고, 모든 화면은 홈의 아이콘에서 엽니다.
탭이 7개일 때는 글자를 8.8px 까지 줄여야 들어갔고(영문 "Balance"·"Alliance" 기준),
눈이 나쁜 사람은 읽지 못했습니다. 격자는 글자를 줄일 이유가 없고, 화면이 늘어도
칸을 하나 더 놓으면 됩니다.

| | 규칙 |
|---|---|
| 새 화면 | `components/HomeTab.tsx` 의 `tiles` 에 칸을 추가 — **하단바를 되살리지 마세요** |
| 아이콘 순서 | 잔액 · 아이템 · 연합 · 레이드 · 내 정보 · 게시판 · 언어 · **관리(맨 마지막)** |
| 관리가 맨 끝인 이유 | 엄지가 닿기 쉬운 자리에 두면 PIN·도구 화면이 잘못 눌린다 |
| 화면 껍데기 | `components/Screen.tsx` (제목 + [✕] + 뒤로가기). 직접 만들지 마세요 |

홈의 "지금 처리할 일" 숫자 중 미분배·지급할 사람은 이미 받아둔 상태에서 세고,
연합·레이드만 따로 읽되 **1분 동안은 다시 읽지 않습니다**(홈은 모든 이동의 길목입니다).
못 읽었으면 **0 으로 보여주지 마세요** — "처리할 일이 없다"는 거짓말이 됩니다 (규칙 7).

**뒤로가기**로 덮인 것(팝업·인증샷 크게보기·홈에서 연 화면)을 닫습니다. 5-9 에서 배경
닫기를 막은 뒤로 이 버튼을 누르는 일이 오히려 늘었는데, 그때 앱을 통째로 벗어나면
**참여자를 스무 명 체크한 것이 그대로 사라집니다.** `lib/back.ts` 한 곳에서만 다룹니다.

| | 규칙 |
|---|---|
| history 항목 | **언제나 최대 한 개**. 덮인 목록이 비어 있지 않은 동안만 표식 하나를 올려둔다 |
| 뒤로가기 | 목록의 **맨 위 하나**만 닫는다. 남아 있으면 표식을 다시 올린다 |
| [✕]·저장 | 목록에서 지우기만 한다 — **`history.back()` 을 부르지 않는다** |
| 홈에서 뒤로가기 | 닫을 것이 없으므로 앱을 나간다 (원래 동작) |

`history.back()` 은 비동기입니다. 다른 이동과 겹치면 back() 과 pushState() 가 같은 순간에
경합해 **한 번에 두 겹이 닫히거나** 뒤로가기가 먹통이 됩니다. 표식을 한 개로 고정하면
그 경합 자체가 없어집니다.

`npm run verify:gs` 가 하단바 부재·아이콘 순서(관리 맨 끝)·글자 크기·모든 화면 도달·
표식 개수·닫히는 순서를 전부 검사합니다.

### 5-10. 인증샷은 앱 안에서 본다 (v11.1)

시트에 저장되는 값은 `https://drive.google.com/file/d/<id>/view` — **이미지가 아니라
드라이브 뷰어 페이지**입니다. `<img>` 에 그대로 넣으면 아무것도 안 나옵니다.
`lib/client.ts` 의 `photoView(url)` 가 썸네일 주소로 바꿉니다.

- 드라이브 링크가 **아니면 그대로 돌려줍니다** — 관리자가 손으로 붙여넣은 다른 주소를
  깨뜨리면 안 됩니다 (규칙 7)
- 못 불러온 사진을 **조용히 숨기지 마세요.** 자리와 함께 "안 열림"을 보여주고 원본
  링크를 남깁니다. 숨기면 사진을 안 붙인 줄로 압니다
- 새 화면에서 사진을 보여줄 때는 `components/PhotoStrip.tsx` 를 쓰세요

### 7. OCR 매칭에 추측을 넣지 않는다

번체/간체 변환(`T2S_MAP`)은 **같은 글자의 다른 서체만** 담습니다. 생긴 게 비슷하다고
다른 뜻의 글자(斬 "베다" vs 惨 "비참하다")를 넣으면 엉뚱한 사람에게 다이아가 갑니다.
매칭 실패의 정답은 항상 "멤버DB D열에 사용자가 정확한 값을 직접 입력"입니다.

---

## 고치고 나서

```bash
npm run verify      # 타입 + 린트 + .gs 검사 + 빌드 + E2E — 전부
```

개별로:

| 명령 | 하는 일 |
|---|---|
| `npm run dev:mock` | 진짜 시트 없이 로컬 실행 (PIN `1234`) — 화면 고칠 때 이것만 쓰면 됨 |
| `npm run verify:gs` | `.gs` 구문·중복·라우터 노출면·락·산식 대조·사용안내·보호훅 + `app/api` 권한 경계 |
| `npm run e2e` | 권한 경계 + 화면 흐름 (`npm run build` 먼저 필요) |
| `E2E_SHOTS=./shots npm run e2e` | 스크린샷까지 남김 |

`.gs` 를 고쳤으면 **`npm run verify:gs` 를 통과하기 전까지 사용자에게 넘기지 마세요.**
구글시트에 붙여넣기 전엔 아무도 실행해보지 않기 때문에, 여기가 마지막 방어선입니다.

새 규칙이 생기면 `scripts/verify-gs.mjs` 의 `check(...)` 를 하나 추가하세요. 문서로만 적어두면
다음 사람이 안 읽습니다.

---

## 자주 하는 실수

| 증상 | 원인 |
|---|---|
| 화면을 고쳤는데 앱이 그대로 | `.gs` 의 HTML 을 고쳤음. `components/` 를 고쳐야 함 |
| `.gs` 를 고쳤는데 앱이 그대로 | 사용자가 [배포 관리] → 새 버전을 안 함 |
| 앱이 로그인 화면을 받음 | Apps Script 배포 액세스가 "모든 사용자"가 아님 |
| 환경변수를 바꿨는데 그대로 | Vercel은 Redeploy 해야 반영됨 |
| 안 건드린 다른 길드 앱까지 갱신됨 | 두 Vercel 프로젝트가 같은 저장소의 `main` 을 봄 (위 항목) |
| Redeploy 했는데 실사용 주소가 그대로 | 미리보기(브랜치) 배포를 다시 돌린 것. **Production** 딱지가 붙은 배포를 골라야 함 |
| 확인 화면 숫자 ≠ 실제 결과 | 산식을 한쪽만 고침 (규칙 1) |
| 등록·분배했는데 숫자가 그대로 | 쓰기 완료 콜백이 `refresh()` (캐시 사용). `refreshNow` 를 써야 함 (규칙 6-2) |
| 중문·영문 화면인데 알림만 한국어 | 시트 결과에 `_rc` 코드를 안 붙였거나 사전에 `s.<code>` 가 없음 (규칙 6-1) |
| 같은 멤버가 두 줄 | 이름을 `_normName` 없이 비교 (규칙 4) |
| 운영비 계정이 두 개 | v10 붙여넣고 `[🏦 혈비 계정을 혈맹운영비로 통일]` 을 안 돌림 |
| 되돌렸는데 잔액이 안 맞음 | 분배내역(O열)을 안 쓰고 현재 비중으로 다시 계산 (규칙 2-1) |

---

## 사용자에게 답할 때

- 한국어로, 전문가 보고서 수준으로.
- 검증을 돌렸으면 **무엇이 몇 건 통과했는지** 밝히세요. "테스트했습니다"로는 부족합니다.
- 비밀값(토큰·PIN·시크릿)을 채팅에 쓰지 말고, 사용자에게 붙여넣어 달라고도 하지 마세요.
  전부 Vercel 대시보드에서 직접 입력하는 값입니다.
- `.gs` 를 바꿨으면 버전을 올리세요: 구조 변경·큰 기능은 정수(v8→v9), 작은 개선은 소수점(v8.0→v8.1).
  헤더 주석 블록, `VERSION` 상수, `_rebuildGuide`(사용안내), `CHANGELOG.md` 를 같은 커밋에서 갱신합니다.
- **시트를 안 건드리는 앱만의 패치(v11.6 → v11.6.1)도 `CHANGELOG.md` 에 한 항목씩 남깁니다.**
  최신이 **맨 위**이고, 같은 minor 안에서 세 번째 자리가 건너뛰면 안 됩니다.
  실제로 v11.6.7~v11.6.10 네 판이 이력 없이 나갔고, v11.6.8 항목은 `>>` 로 덧붙는 바람에
  파일 **끝** 주석의 `-->` 에 한 줄로 붙어(`-->## v11.6.8`) 제목으로 읽히지도 않았습니다.
  `npm run verify:gs` 가 이 세 가지(맨 위=앱 버전 · 빠진 판 없음 · 붙은 제목 없음)를 검사합니다.
  이력 본문은 백틱이 많으니 셸 리다이렉트 말고 **편집 도구로** 넣으세요.
